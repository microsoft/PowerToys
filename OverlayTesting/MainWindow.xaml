<Window x:Class="OverlayTesting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="800"
        Height="200">
    <Grid>
        <Border x:Name="OverlayPill"
                Background="#CC1E1E2F"
                CornerRadius="6"
                Padding="10,6"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,24,24,0"
                Opacity="0">
            <TextBlock x:Name="OverlayText"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Text="Ctrl + T" />
        </Border>
    </Grid>
</Window>
