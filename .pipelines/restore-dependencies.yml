steps:
  - task: BatchScript@2
    displayName: 'Install Windows All dependencies via Visual Studio'
    inputs:
      filePath: .\restore-dependencies.cmd
      arguments: ${{ parameters.sdkVersion }}
