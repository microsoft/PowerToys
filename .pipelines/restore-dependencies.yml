parameters:
  sdkVersion: 17134

steps:
  - task: powershell@2
    inputs:
      targetType: filePath
      filePath: .\restore-windowsSdk17134.ps1
      arguments: ${{ parameters.sdkVersion }}
    displayName: 'Install Windows SDK (${{ parameters.sdkVersion }})'