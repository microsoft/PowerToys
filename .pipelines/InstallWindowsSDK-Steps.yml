parameters:
  sdkVersion: 17134

steps:
  - task: powershell@2
    inputs:
      targetType: filePath
      filePath: tools\build\Install-WindowsSdkISO.ps1
      arguments: ${{ parameters.sdkVersion }}
    displayName: 'Install Windows SDK (${{ parameters.sdkVersion }})'
