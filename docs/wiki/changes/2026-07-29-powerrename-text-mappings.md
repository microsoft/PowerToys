# Support plain-text numeric rename mappings

**Date:** 2026-07-29

PowerRename's numeric mapping picker now accepts `.txt` files in addition to CSV and Excel files. Each non-empty line is one replacement name, mapped in order to numeric filenames starting with `1`. Text lines are literal, so commas do not need CSV quoting.
