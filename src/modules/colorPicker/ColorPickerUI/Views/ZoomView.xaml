<UserControl
    x:Class="ColorPicker.Views.ZoomView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Border
        Margin="12"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Background="{ThemeResource LayerFillColorDefaultBrush}"
        BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="{ThemeResource ControlCornerRadius}">
        <!--
            Approach A / 方案1: a single Win2D CanvasControl draws the zoomed (nearest-neighbor)
            capture plus the pixel grid, replacing the WPF ShaderEffect. See GridShader.fx for
            the original grid spec; the brightness-adaptive color + radius reveal are simplified
            here and can be refined (or escalated to 方案2) after desktop verification.
        -->
        <canvas:CanvasControl
            x:Name="ZoomCanvas"
            Margin="3"
            Draw="ZoomCanvas_Draw" />
    </Border>
</UserControl>
