<UserControl
    x:Class="ColorPicker.Views.ZoomView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Border
        x:Name="ZoomCard"
        Margin="12"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Background="{ThemeResource LayerFillColorDefaultBrush}"
        BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="{ThemeResource ControlCornerRadius}">
        <!--
            A single Win2D CanvasControl draws the zoomed (nearest-neighbor) capture plus a
            brightness-adaptive pixel grid + center highlight, replacing the WPF ShaderEffect
            (方案2: the grid color adapts to local pixel brightness and fades toward the edges).
        -->
        <canvas:CanvasControl
            x:Name="ZoomCanvas"
            Margin="3"
            Draw="ZoomCanvas_Draw" />
    </Border>
</UserControl>
