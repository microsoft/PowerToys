<UserControl x:Class="ColorPicker.Controls.ColorFormatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorPicker.Controls"
              xmlns:e="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:behaviors="clr-namespace:ColorPicker.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border
                    Margin="0,12,0,0"
                    Width="308"
                    Height="36"
                    CornerRadius="4"
                    Padding="8"
                    HorizontalAlignment="Stretch"
                    Background="{DynamicResource ColorControlBackgroundBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="FormatNameTextBox" Opacity="0.4" Foreground="{DynamicResource PrimaryForegroundBrush}" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center"/>
            <TextBlock x:Name="ColorTextRepresentationTextBox" Text="{Binding Text}" Foreground="{DynamicResource PrimaryForegroundBrush}" FontFamily="Consolas" Grid.Column="1" FontSize="14" Margin="0,-2,0,0" VerticalAlignment="Center"/>
        </Grid>
        <Border.Effect>
            <DropShadowEffect BlurRadius="6" Opacity="0.32" ShadowDepth="2" />
        </Border.Effect>
    </Border>
</UserControl>
