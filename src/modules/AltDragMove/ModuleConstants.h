#pragma once

namespace NonLocalizable
{
    const inline wchar_t ModuleKey[] = L"AltDragMove";
    const inline wchar_t ModuleDescription[] = L"Alt+Drag to move windows by grabbing anywhere in the window.";
}
