<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="KeyboardManagerEditorUI.Pages.URLs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:KeyboardManagerEditorUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="using:KeyboardManagerEditorUI.Helpers"
    xmlns:local="using:KeyboardManagerEditorUI.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <StackPanel Orientation="Vertical" Spacing="12">
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{x:Bind helper:ResourceLoaderInstance.GetString('UrlPage_Instruction')}"
                TextWrapping="Wrap" />
            <Button
                x:Name="NewShortcutBtn"
                Height="36"
                Margin="0,12,0,0"
                Click="NewShortcutBtn_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon
                        FontSize="14"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        Glyph="&#xE109;" />
                    <TextBlock VerticalAlignment="Center" Text="{x:Bind helper:ResourceLoaderInstance.GetString('UrlPage_New')}" />
                </StackPanel>
            </Button>

            <Grid
                HorizontalAlignment="Stretch"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{ThemeResource OverlayCornerRadius}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="48" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="348" />
                    <ColumnDefinition Width="236" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="84" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Margin="16,-2,0,0"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind helper:ResourceLoaderInstance.GetString('UrlPage_Shortcut')}" />
                <AppBarSeparator
                    Grid.Column="1"
                    Margin="-6,4,0,4"
                    HorizontalAlignment="Left" />
                <TextBlock
                    Grid.Column="1"
                    Margin="12,-2,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind helper:ResourceLoaderInstance.GetString('UrlPage_Url')}" />
                <Rectangle
                    Grid.ColumnSpan="4"
                    Height="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Bottom"
                    Fill="{ThemeResource CardStrokeColorDefaultBrush}" />
                <ListView
                    Grid.Row="1"
                    Grid.ColumnSpan="4"
                    IsItemClickEnabled="True"
                    ItemClick="ListView_ItemClick"
                    ItemsSource="{x:Bind Shortcuts}"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="helper:URLShortcut">
                            <Grid Height="48">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="348" />
                                    <ColumnDefinition Width="476" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="84" />
                                </Grid.ColumnDefinitions>
                                <Rectangle
                                    Grid.ColumnSpan="5"
                                    Height="1"
                                    Margin="-16,0,-16,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Bottom"
                                    Fill="{ThemeResource CardStrokeColorDefaultBrush}"
                                    Opacity="0.8" />
                                <ItemsControl
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    ItemsSource="{x:Bind Shortcut}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="4" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <controls:KeyVisual
                                                HorizontalAlignment="Left"
                                                Content="{Binding}"
                                                Style="{StaticResource DefaultKeyVisualStyle}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <HyperlinkButton
                                    Grid.Column="1"
                                    Margin="-12,0,0,0"
                                    Content="{x:Bind URL}" />
                                <Button
                                    Grid.ColumnSpan="4"
                                    Margin="0,0,4,0"
                                    Padding="8,4"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    AutomationProperties.Name="Delete URL shortcut"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Click="DeleteButton_Click"
                                    ToolTipService.ToolTip="{x:Bind helper:ResourceLoaderInstance.GetString('RemappingsPage_DeleteTooltip')}">
                                    <FontIcon
                                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                                        FontSize="16"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Glyph="&#xE74D;" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </StackPanel>
        <ContentDialog
            x:Name="KeyDialog"
            Title="{x:Bind helper:ResourceLoaderInstance.GetString('UrlPage_KeyDialogTitle')}"
            Width="480"
            Height="360"
            MinWidth="600"
            MaxWidth="600"
            PrimaryButtonClick="KeyDialog_PrimaryButtonClick"
            PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
            PrimaryButtonText="{x:Bind helper:ResourceLoaderInstance.GetString('UrlPage_KeyDialogPrimary')}"
            SecondaryButtonText="{x:Bind helper:ResourceLoaderInstance.GetString('UrlPage_KeyDialogSecondary')}">
            <Grid>
                <controls:UrlPageInputControl x:Name="UrlShortcutControl" />
            </Grid>
        </ContentDialog>
    </Grid>
</Page>
