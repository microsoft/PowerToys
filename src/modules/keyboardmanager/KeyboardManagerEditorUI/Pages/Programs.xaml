<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="KeyboardManagerEditorUI.Pages.Programs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="using:KeyboardManagerEditorUI.Helpers"
    xmlns:local="using:KeyboardManagerEditorUI.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:styles="using:KeyboardManagerEditorUI.Styles"
    mc:Ignorable="d">

    <Grid Padding="16">
        <StackPanel Orientation="Vertical" Spacing="12">
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Instruction')}"
                TextWrapping="Wrap" />
            <Button
                x:Name="NewShortcutBtn"
                Height="36"
                Margin="0,12,0,0"
                Click="NewShortcutBtn_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon
                        FontSize="14"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        Glyph="&#xE109;" />
                    <TextBlock 
                        VerticalAlignment="Center" 
                        Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_New')}" />
                </StackPanel>
            </Button>
            <Grid
                HorizontalAlignment="Stretch"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource OverlayCornerRadius}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="48" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="236" />
                    <ColumnDefinition Width="236" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="84" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Margin="16,-2,0,0"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Shortcut')}" />
                <AppBarSeparator
                    Grid.Column="1"
                    Margin="-6,4,0,4"
                    HorizontalAlignment="Left" />
                <TextBlock
                    Grid.Column="1"
                    Margin="12,-2,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Program')}" />
                <Rectangle
                    Grid.ColumnSpan="4"
                    Height="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Bottom"
                    Fill="{ThemeResource CardStrokeColorDefaultBrush}" />
                <ListView
                    Grid.Row="1"
                    Grid.ColumnSpan="4"
                    IsItemClickEnabled="True"
                    ItemClick="ListView_ItemClick"
                    ItemsSource="{x:Bind Shortcuts}"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="helper:URLShortcut">
                            <Grid Height="48">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="232" />
                                    <ColumnDefinition Width="238" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="84" />
                                </Grid.ColumnDefinitions>
                                <Rectangle
                                    Grid.ColumnSpan="5"
                                    Height="1"
                                    Margin="-16,0,-16,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Bottom"
                                    Fill="{ThemeResource CardStrokeColorDefaultBrush}"
                                    Opacity="0.8" />
                                <ItemsControl
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    ItemsSource="{x:Bind Shortcut}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="4" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <styles:KeyVisual
                                                HorizontalAlignment="Left"
                                                Content="{Binding}"
                                                VisualType="SmallOutline" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <StackPanel
                                    Grid.Column="1"
                                    Orientation="Horizontal"
                                    Spacing="8">
                                    <TextBlock VerticalAlignment="Center" Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_ExampleApp')}" />
                                    <FontIcon
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE946;">
                                        <ToolTipService.ToolTip>
                                            <TextBlock>
                                                <Run FontWeight="SemiBold" Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Arguments')}" />
                                                <Run Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Args')}" />
                                                <LineBreak />
                                                <Run FontWeight="SemiBold" Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_StartDir')}" />
                                                <Run Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_StartDirVal')}" />
                                                <LineBreak />
                                                <Run FontWeight="SemiBold" Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_LaunchState')}" />
                                                <Run Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_LaunchStateVal')}" />
                                            </TextBlock>
                                        </ToolTipService.ToolTip>
                                    </FontIcon>
                                </StackPanel>
                                <ToggleSwitch
                                    Grid.ColumnSpan="4"
                                    Margin="0,0,-112,0"
                                    HorizontalAlignment="Right"
                                    IsOn="True"
                                    OffContent=""
                                    OnContent="" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </StackPanel>
        <ContentDialog
            x:Name="KeyDialog"
            Title="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_KeyDialogTitle')}"
            Width="480"
            MinWidth="600"
            MaxWidth="600"
            PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
            PrimaryButtonText="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_KeyDialogPrimary')}"
            SecondaryButtonText="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_KeyDialogSecondary')}">
            <StackPanel Width="480" Orientation="Vertical">
                <TextBlock Margin="0,12,0,8" Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Shortcut')}" />
                <ToggleButton
                    x:Name="OriginalToggleBtn"
                    Padding="0,24,0,24"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource CustomShortcutToggleButtonStyle}">
                    <ToggleButton.Content>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <styles:KeyVisual Content="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_ExampleKey1')}" VisualType="SmallOutline" />
                            <styles:KeyVisual Content="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_ExampleKey2')}" VisualType="SmallOutline" />
                        </StackPanel>
                    </ToggleButton.Content>
                </ToggleButton>
                <Grid
                    Margin="0,24,0,0"
                    ColumnSpacing="4"
                    RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBox 
                        Header="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_ExampleHeader')}"
                        Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_ExampleApp')}" />
                    <Button
                        Grid.Column="1"
                        Height="32"
                        VerticalAlignment="Bottom">
                        <FontIcon FontSize="14" Glyph="&#xE8DA;" />
                    </Button>
                    <TextBox
                        Grid.Row="1"
                        Header="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Arguments')}"
                        Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_Args')}" />
                    <TextBox
                        Grid.Row="2"
                        Header="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_KeyDialogStartHeader')}"
                        Text="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_KeyDialogStartVal')}" />
                    <Button
                        Grid.Row="2"
                        Grid.Column="1"
                        Height="32"
                        VerticalAlignment="Bottom">
                        <FontIcon FontSize="14" Glyph="&#xE8DA;" />
                    </Button>
                    <ComboBox
                        Grid.Row="3"
                        Header="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_KeyDialogLaunchHeader')}"
                        SelectedIndex="0">
                        <ComboBoxItem Content="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_KeyDialogLaunchShow')}" />
                        <ComboBoxItem Content="{x:Bind helper:ResourceLoaderInstance.GetString('ProgramsPage_LaunchStateVal')}" />
                    </ComboBox>
                </Grid>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>
