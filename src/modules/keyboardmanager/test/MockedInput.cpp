#include "pch.h"
#include "MockedInput.h"

UINT MockedInput::SendVirtualInput(UINT cInputs, LPINPUT pInputs, int cbSize)
{
    // iterate over inputs
    // Trigger low level hook
    // set mocked keyboard state
    return 1;
}
