/**
 * Recursively remove a temporary directory.
 * Fails silently if the directory doesn't exist or can't be removed.
 */
export declare function cleanup(tempDir: string): void;
//# sourceMappingURL=stage-cleanup.d.ts.map