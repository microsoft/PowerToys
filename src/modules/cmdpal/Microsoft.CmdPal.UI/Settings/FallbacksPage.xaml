<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Microsoft.CmdPal.UI.Settings.FallbacksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:InteractivityCore="using:Microsoft.Xaml.Interactions.Core"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:cpcontrols="using:Microsoft.CmdPal.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Microsoft.CmdPal.UI.Helpers"
    xmlns:local="using:Microsoft.CmdPal.UI.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:viewModels="using:Microsoft.CmdPal.UI.ViewModels"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="1">
            <Grid Padding="16">
                <StackPanel
                    MaxWidth="1000"
                    HorizontalAlignment="Stretch"
                    Spacing="{StaticResource SettingsCardSpacing}">

                    <ListView
                        AllowDrop="True"
                        CanDragItems="True"
                        CanReorderItems="True"
                        ItemsSource="{x:Bind viewModel.FallbackSettings, Mode=OneWay}"
                        DragItemsCompleted="ListView_DragItemsCompleted"
                        SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:FallbackSettingsViewModel">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <!--  Provide a custom area that can be manipulated by the user, this could be before or after the card.  -->
                                    <Border
                                        Width="36"
                                        Height="75"
                                        Margin="1,1,1,1"
                                        VerticalAlignment="Top"
                                        Background="Transparent"
                                        CornerRadius="{ThemeResource ControlCornerRadius}">
                                        <TextBlock
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontFamily="Segoe UI Symbol"
                                            FontSize="20"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="&#x283F;" />
                                        <!--  Use Behaviors to hover on pointer  -->
                                        <Interactivity:Interaction.Behaviors>
                                            <InteractivityCore:EventTriggerBehavior EventName="PointerEntered">
                                                <InteractivityCore:ChangePropertyAction PropertyName="Background">
                                                    <InteractivityCore:ChangePropertyAction.Value>
                                                        <SolidColorBrush Color="{ThemeResource ControlFillColorDefault}" />
                                                    </InteractivityCore:ChangePropertyAction.Value>
                                                </InteractivityCore:ChangePropertyAction>
                                            </InteractivityCore:EventTriggerBehavior>
                                            <InteractivityCore:EventTriggerBehavior EventName="PointerExited">
                                                <InteractivityCore:ChangePropertyAction PropertyName="Background">
                                                    <InteractivityCore:ChangePropertyAction.Value>
                                                        <SolidColorBrush Color="Transparent" />
                                                    </InteractivityCore:ChangePropertyAction.Value>
                                                </InteractivityCore:ChangePropertyAction>
                                            </InteractivityCore:EventTriggerBehavior>
                                        </Interactivity:Interaction.Behaviors>
                                    </Border>
                                    <!--  Standard Settings Expander (could also just be a Card)  -->
                                    <controls:SettingsExpander
                                        Grid.Column="1"
                                        Description="{x:Bind ExtensionName, Mode=OneWay}"
                                        Header="{x:Bind DisplayName, Mode=OneWay}"
                                        IsExpanded="False">
                                        <controls:SettingsExpander.HeaderIcon>
                                            <cpcontrols:ContentIcon>
                                                <cpcontrols:ContentIcon.Content>
                                                    <cpcontrols:IconBox
                                                        Width="20"
                                                        Height="20"
                                                        AutomationProperties.AccessibilityView="Raw"
                                                        SourceKey="{x:Bind Icon, Mode=OneWay}"
                                                        SourceRequested="{x:Bind helpers:IconCacheProvider.SourceRequested}" />
                                                </cpcontrols:ContentIcon.Content>
                                            </cpcontrols:ContentIcon>
                                        </controls:SettingsExpander.HeaderIcon>

                                        <ToggleSwitch
                                            IsOn="{x:Bind IsEnabled, Mode=TwoWay}" />

                                        <controls:SettingsExpander.Items>
                                            <controls:SettingsCard ContentAlignment="Left">
                                                <cpcontrols:CheckBoxWithDescriptionControl x:Uid="Settings_FallbacksPage_GlobalResults_SettingsCard" IsChecked="{x:Bind IncludeInGlobalResults, Mode=TwoWay}" />
                                            </controls:SettingsCard>
                                        </controls:SettingsExpander.Items>
                                    </controls:SettingsExpander>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <!--  Customize Size of Item Container from ListView  -->
                        <ListView.ItemContainerStyle>
                            <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Padding" Value="4" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <!--  Hides the overall highlight from the ListView Container  -->
                        <ListView.Resources>
                            <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="Transparent" />
                        </ListView.Resources>
                    </ListView>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>