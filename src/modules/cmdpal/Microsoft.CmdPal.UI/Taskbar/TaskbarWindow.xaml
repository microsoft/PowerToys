<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx
    x:Class="Microsoft.CmdPal.UI.Taskbar.TaskbarWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Microsoft.CmdPal.UI.Taskbar"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    Title="CmdPal Taskbar"
    IsAlwaysOnTop="True"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <winuiex:TransparentTintBackdrop />
    </Window.SystemBackdrop>

    <Grid
        x:Name="Root"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Background="Transparent">

        <!--
            Horizontal layout uses columns: Logo | Buttons | Mid(*) | Content | Tray
            Vertical layout uses rows:    Buttons | Mid(*) | Content | Tray
            The inactive axis collapses to a single stretch dimension.
            Code-behind calls ApplyOrientation() to switch.
        -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="WindowsLogoCol" Width="0" />
            <ColumnDefinition x:Name="ButtonsCol" Width="0" />
            <ColumnDefinition x:Name="MidCol" Width="*" />
            <ColumnDefinition x:Name="ContentCol" Width="Auto" />
            <ColumnDefinition x:Name="TrayCol" Width="400" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="ButtonsRow" Height="0" />
            <RowDefinition x:Name="MidRow" Height="*" />
            <RowDefinition x:Name="ContentRow" Height="Auto" />
            <RowDefinition x:Name="TrayRow" Height="0" />
        </Grid.RowDefinitions>

        <ContentControl
            x:Name="MainContent"
            Grid.Row="0"
            Grid.RowSpan="4"
            Grid.Column="3"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            SizeChanged="MainContent_SizeChanged" />
    </Grid>
</winuiex:WindowEx>
