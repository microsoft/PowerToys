<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Microsoft.CmdPal.UI.TabbedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:cpcontrols="using:Microsoft.CmdPal.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:help="using:Microsoft.CmdPal.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Microsoft.CmdPal.UI.ViewModels"
    Background="Transparent"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

            <!--  Template for a single tab in the strip: optional icon, title, optional badge pill  -->
            <DataTemplate x:Key="TabItemTemplate" x:DataType="viewModels:TabViewModel">
                <StackPanel
                    AutomationProperties.Name="{x:Bind Title, Mode=OneWay}"
                    Orientation="Horizontal"
                    Spacing="8">
                    <cpcontrols:IconBox
                        Width="16"
                        Height="16"
                        VerticalAlignment="Center"
                        AutomationProperties.AccessibilityView="Raw"
                        SourceKey="{x:Bind Icon, Mode=OneWay}"
                        SourceRequested="{x:Bind help:IconProvider.SourceRequested20}"
                        Visibility="{x:Bind HasIcon, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock VerticalAlignment="Center" Text="{x:Bind Title, Mode=OneWay}" />
                    <Border
                        Padding="6,0"
                        VerticalAlignment="Center"
                        Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                        CornerRadius="10"
                        Visibility="{x:Bind HasBadge, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock
                            VerticalAlignment="Center"
                            AutomationProperties.Name="{x:Bind Badge, Mode=OneWay}"
                            FontSize="11"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind Badge, Mode=OneWay}" />
                    </Border>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.KeyboardAccelerators>
            <KeyboardAccelerator
                Key="Tab"
                Invoked="NextTab_Invoked"
                Modifiers="Control" />
            <KeyboardAccelerator
                Key="Tab"
                Invoked="PreviousTab_Invoked"
                Modifiers="Control,Shift" />
        </Grid.KeyboardAccelerators>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Tab strip  -->
        <ListView
            x:Name="TabStrip"
            Grid.Row="0"
            Padding="8,4"
            IsItemClickEnabled="False"
            ItemTemplate="{StaticResource TabItemTemplate}"
            ItemsSource="{x:Bind ViewModel.Tabs, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedTab, Mode=TwoWay}"
            SelectionMode="Single"
            Visibility="{x:Bind ViewModel.HasTabs, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                    <Setter Property="MinHeight" Value="0" />
                    <Setter Property="MinWidth" Value="0" />
                    <Setter Property="Padding" Value="12,6" />
                    <Setter Property="Margin" Value="2,0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemContainerTransitions>
                <TransitionCollection />
            </ListView.ItemContainerTransitions>
        </ListView>

        <!--  Per-tab progress bar: reflects the active tab's page loading, distinct from the shell-level bar  -->
        <ProgressBar
            Grid.Row="1"
            VerticalAlignment="Top"
            IsIndeterminate="True"
            Visibility="{x:Bind ViewModel.ActiveTabIsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

        <!--  Active tab content  -->
        <Grid Grid.Row="2">
            <Frame x:Name="TabFrame" />
            <TextBlock
                x:Name="UnsupportedPlaceholder"
                x:Uid="TabbedPage_UnsupportedTab"
                Margin="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextAlignment="Center"
                TextWrapping="Wrap"
                Visibility="{x:Bind ViewModel.ShowUnsupportedPlaceholder, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>
    </Grid>
</Page>
