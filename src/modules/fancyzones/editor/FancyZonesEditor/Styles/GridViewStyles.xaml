<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        The WPF editor re-templated GridViewItem to hand-draw selection, pointer-over and
        focus visuals. WinUI's DefaultGridViewItemStyle already renders all of those states,
        so these styles only carry the card chrome. Selection state and the automation
        name/id (which the WPF styles set with {Binding} inside a Setter - unsupported in
        WinUI) are applied per-container from MainWindow.ContainerContentChanging.
    -->
    <Style
        x:Key="MonitorItemContainerStyle"
        BasedOn="{StaticResource DefaultGridViewItemStyle}"
        TargetType="GridViewItem">
        <Setter Property="Background" Value="{ThemeResource LayoutItemBackgroundBrush}" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="8" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="FocusVisualMargin" Value="-2" />
    </Style>

    <Style
        x:Key="LayoutItemContainerStyle"
        BasedOn="{StaticResource DefaultGridViewItemStyle}"
        TargetType="GridViewItem">
        <Setter Property="Background" Value="{ThemeResource LayoutItemBackgroundBrush}" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="8" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="FocusVisualMargin" Value="-2" />
    </Style>
</ResourceDictionary>
