<local:EditorWindow
    x:Class="FancyZonesEditor.GridEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FancyZonesEditor"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:props="clr-namespace:FancyZonesEditor.Properties"
    Title=""
    MinWidth="420"
    AutomationProperties.Name="{x:Static props:Resources.Grid_Layout_Editor}"
    Closed="OnClosed"
    ContentRendered="EditorWindow_ContentRendered"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Background="{DynamicResource LayerFillColorAltBrush}">
            <StackPanel Margin="16">
                <TextBlock
                    Focusable="True"
                    FontSize="12"
                    TextWrapping="Wrap">
                    <Run FontWeight="SemiBold" Text="{x:Static props:Resources.SplitterName}" />
                    <LineBreak />
                    <Run Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static props:Resources.SplitterDescription}" />
                </TextBlock>
                <TextBlock
                    Margin="0,16,0,0"
                    Focusable="True"
                    FontSize="12"
                    TextWrapping="Wrap">
                    <Run FontWeight="SemiBold" Text="{x:Static props:Resources.MergeName}" />
                    <LineBreak />
                    <Run Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static props:Resources.MergeDescription}" />
                </TextBlock>
                <TextBlock
                    Margin="0,16,0,0"
                    Focusable="True"
                    FontSize="12"
                    TextWrapping="Wrap">
                    <Run FontWeight="SemiBold" Text="{x:Static props:Resources.KeyboardControlsName}" />
                    <LineBreak />
                    <Run Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static props:Resources.KeyboardControlsDescription}" />
                </TextBlock>
            </StackPanel>
        </Grid>
        <Rectangle
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            Fill="{DynamicResource CardStrokeColorDefaultBrush}" />
        <Grid Grid.Row="2" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                Click="OnCancel"
                Content="{x:Static props:Resources.Cancel}"
                TabIndex="1" />
            <Button
                HorizontalAlignment="Stretch"
                Click="OnSave"
                Content="{x:Static props:Resources.Save}"
                Style="{StaticResource AccentButtonStyle}"
                TabIndex="0" />
        </Grid>
    </Grid>
</local:EditorWindow>