<UserControl
    x:Class="FancyZonesEditor.CanvasZone"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:FancyZonesEditor.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:FancyZonesEditor.Helpers"
    xmlns:local="using:FancyZonesEditor"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    IsTabStop="True"
    KeyDown="Zone_KeyDown"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="CanvasZoneThumbStyle" TargetType="Thumb">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border
                            x:Name="ThumbBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{ThemeResource AccentFillColorDefaultBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"
                            Opacity="0">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualStateGroup.Transitions>
                                        <VisualTransition GeneratedDuration="0:0:0.15">
                                            <VisualTransition.GeneratedEasingFunction>
                                                <ExponentialEase EasingMode="EaseInOut" />
                                            </VisualTransition.GeneratedEasingFunction>
                                        </VisualTransition>
                                    </VisualStateGroup.Transitions>
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="ThumbBorder"
                                                Storyboard.TargetProperty="Opacity"
                                                To="1"
                                                Duration="0:0:0.15" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="ThumbBorder"
                                                Storyboard.TargetProperty="Opacity"
                                                To="1"
                                                Duration="0:0:0.15" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{ThemeResource PrimaryForegroundBrush}" />
            <Setter Property="Padding" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter
                                x:Name="ContentPresenter"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                Foreground="{TemplateBinding Foreground}" />

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Opacity" Value="0.6" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Opacity" Value="0.4" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="ContentPresenter.Opacity" Value="0.4" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border
        x:Name="RootBorder"
        Background="{ThemeResource CanvasZoneBackgroundBrush}"
        BorderBrush="{ThemeResource CanvasZoneBorderBrush}"
        BorderThickness="1"
        CornerRadius="0">
        <Grid x:Name="Frame">
            <Grid.RowDefinitions>
                <RowDefinition Height="8" />
                <RowDefinition Height="16" />
                <RowDefinition Height="*" />
                <RowDefinition Height="16" />
                <RowDefinition Height="8" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="8" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Row="2"
                Grid.Column="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Vertical">
                <TextBlock
                    x:Name="LabelID"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    x:FieldModifier="public"
                    FontSize="64"
                    FontWeight="SemiBold"
                    Foreground="{x:Bind RootBorder.BorderBrush, Mode=OneWay}"
                    Text="ID" />
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Foreground="{x:Bind RootBorder.BorderBrush, Mode=OneWay}"
                    Opacity="0.6">
                    <Run x:Name="WidthLabel" />
                    <Run
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        FontSize="10"
                        Text="&#xE947;" />
                    <Run x:Name="HeightLabel" />
                </TextBlock>
            </StackPanel>

            <!--
                WinUI's Thumb is sealed, so the resize cursor is carried by the
                CursorContentControl wrapper around each drag handle.
            -->
            <controls:CursorContentControl
                Grid.Row="0"
                Grid.RowSpan="5"
                Grid.Column="0"
                Grid.ColumnSpan="5"
                Margin="-1"
                CursorShape="SizeAll">
                <Thumb
                    Padding="4"
                    Background="Transparent"
                    BorderThickness="3"
                    DragDelta="UniversalDragDelta"
                    DragStarted="Caption_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>

            <controls:CursorContentControl Grid.ColumnSpan="5" CursorShape="SizeNorthSouth">
                <Thumb
                    BorderThickness="0,2,0,0"
                    DragDelta="UniversalDragDelta"
                    DragStarted="NResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>
            <controls:CursorContentControl
                Grid.Row="4"
                Grid.ColumnSpan="5"
                CursorShape="SizeNorthSouth">
                <Thumb
                    BorderThickness="0,0,0,2"
                    DragDelta="UniversalDragDelta"
                    DragStarted="SResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>
            <controls:CursorContentControl Grid.RowSpan="5" CursorShape="SizeWestEast">
                <Thumb
                    BorderThickness="2,0,0,0"
                    DragDelta="UniversalDragDelta"
                    DragStarted="WResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>
            <controls:CursorContentControl
                Grid.RowSpan="5"
                Grid.Column="4"
                CursorShape="SizeWestEast">
                <Thumb
                    BorderThickness="0,0,2,0"
                    DragDelta="UniversalDragDelta"
                    DragStarted="EResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>

            <controls:CursorContentControl
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                CursorShape="SizeNorthwestSoutheast">
                <Thumb
                    BorderThickness="2,2,0,0"
                    DragDelta="UniversalDragDelta"
                    DragStarted="NWResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>
            <controls:CursorContentControl
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="3"
                Grid.ColumnSpan="2"
                CursorShape="SizeNortheastSouthwest">
                <Thumb
                    AutomationProperties.AutomationId="NEResize"
                    BorderThickness="0,2,2,0"
                    DragDelta="UniversalDragDelta"
                    DragStarted="NEResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>
            <controls:CursorContentControl
                Grid.Row="3"
                Grid.RowSpan="2"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                CursorShape="SizeNortheastSouthwest">
                <Thumb
                    BorderThickness="2,0,0,2"
                    DragDelta="UniversalDragDelta"
                    DragStarted="SWResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>
            <controls:CursorContentControl
                Grid.Row="3"
                Grid.RowSpan="2"
                Grid.Column="3"
                Grid.ColumnSpan="2"
                CursorShape="SizeNorthwestSoutheast">
                <Thumb
                    BorderThickness="0,0,2,2"
                    DragDelta="UniversalDragDelta"
                    DragStarted="SEResize_DragStarted"
                    Style="{StaticResource CanvasZoneThumbStyle}" />
            </controls:CursorContentControl>

            <Button
                Grid.Row="2"
                Grid.Column="2"
                Padding="4"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="OnClose"
                Content="&#xE894;"
                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                FontSize="16"
                IsTabStop="False"
                Style="{StaticResource CloseButtonStyle}"
                ToolTipService.ToolTip="{helpers:StringResource Key=Delete_Zone}" />

            <Canvas x:Name="Body" />
        </Grid>
    </Border>
</UserControl>
