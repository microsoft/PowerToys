<?xml version="1.0" encoding="utf-8" ?>
<common:TransparentWindow
    x:Class="PowerAccent.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:Microsoft.PowerToys.Common.UI.Controls.Window"
    xmlns:controls="using:Microsoft.PowerToys.Common.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:PowerAccent.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <controls:TransientSurface x:Name="Surface" Margin="24,24,24,16">
        <Grid x:Name="RootContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}">
                <ListView
                    x:Name="CharactersList"
                    Padding="0"
                    HorizontalAlignment="Center"
                    AutomationProperties.AutomationId="QuickAccentCharacterList"
                    IsHitTestVisible="False"
                    IsItemClickEnabled="False"
                    ItemsSource="{x:Bind ViewModel.Characters, Mode=OneWay}"
                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                    ScrollViewer.HorizontalScrollMode="Enabled"
                    ScrollViewer.VerticalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollMode="Disabled"
                    SelectionMode="Single">
                    <!--
                        Keep the stock ListViewItem template but recolor the selected state to the accent
                        "pill" look (the default selected fill is a subtle gray). Overridden via
                        ThemeDictionaries - not a flat alias - because the accent / on-accent brushes differ
                        between light and dark, so the selection retints correctly on a live theme switch.
                    -->
                    <ListView.Resources>
                        <ResourceDictionary>
                            <ResourceDictionary.ThemeDictionaries>
                                <ResourceDictionary x:Key="Default">
                                    <StaticResource x:Key="ListViewItemBackgroundSelected" ResourceKey="AccentFillColorDefaultBrush" />
                                    <StaticResource x:Key="ListViewItemBackgroundSelectedPointerOver" ResourceKey="AccentFillColorSecondaryBrush" />
                                    <StaticResource x:Key="ListViewItemBackgroundSelectedPressed" ResourceKey="AccentFillColorTertiaryBrush" />
                                    <StaticResource x:Key="ListViewItemForegroundSelected" ResourceKey="TextOnAccentFillColorPrimaryBrush" />
                                </ResourceDictionary>
                                <ResourceDictionary x:Key="Light">
                                    <StaticResource x:Key="ListViewItemBackgroundSelected" ResourceKey="AccentFillColorDefaultBrush" />
                                    <StaticResource x:Key="ListViewItemBackgroundSelectedPointerOver" ResourceKey="AccentFillColorSecondaryBrush" />
                                    <StaticResource x:Key="ListViewItemBackgroundSelectedPressed" ResourceKey="AccentFillColorTertiaryBrush" />
                                    <StaticResource x:Key="ListViewItemForegroundSelected" ResourceKey="TextOnAccentFillColorPrimaryBrush" />
                                </ResourceDictionary>
                            </ResourceDictionary.ThemeDictionaries>
                        </ResourceDictionary>
                    </ListView.Resources>
                    <!--
                        Disable the default ListView item animations (entrance / reorder /
                        reposition). The accent bar is rebuilt and re-selected on every keystroke;
                        the built-in transitions make glyphs visibly slide/fade in, which reads as
                        lag on a typing aid that must feel instant.
                    -->
                    <ListView.ItemContainerTransitions>
                        <TransitionCollection />
                    </ListView.ItemContainerTransitions>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <!--
                        Use the stock ListViewItem selection chrome (ListViewItemPresenter) instead of a
                        custom "pill" ControlTemplate. Only the layout-related setters are kept so the bar
                        still hugs its content: 48px square cells with centered content and no extra margin.
                    -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="IsTabStop" Value="False" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <!--
                                WinUI's ListViewItem defaults MinWidth to 88 (ListViewItemMinWidth); WPF's
                                ListBoxItem defaulted to 0. Without this override each 48px glyph cell is
                                padded out to 88px, leaving wide gaps between accents. Pin it to the cell
                                width so the bar is as tight as the WPF original.
                            -->
                            <Setter Property="MinWidth" Value="48" />
                            <Setter Property="Height" Value="48" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="18"
                                Text="{x:Bind Mode=OneTime}"
                                TextAlignment="Center" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>


            </Grid>
            <Grid Grid.Row="1" Visibility="{x:Bind ViewModel.DescriptionVisibility, Mode=OneWay}">
                <TextBlock
                    x:Name="CharacterName"
                    MaxHeight="36"
                    Margin="8"
                    AutomationProperties.AutomationId="QuickAccentDescription"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind ViewModel.Description, Mode=OneWay}"
                    TextAlignment="Center"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="Wrap" />
                <Rectangle
                    Height="1"
                    VerticalAlignment="Top"
                    Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
            </Grid>
        </Grid>
    </controls:TransientSurface>
</common:TransparentWindow>
