<UserControl
    x:Class="ImageResizer.Views.InputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:m="using:ImageResizer.Models"
    xmlns:v="using:ImageResizer.Views">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="16">
            <ComboBox
                Name="SizeComboBox"
                Height="64"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                ItemsSource="{Binding Settings.AllSizes}"
                SelectedItem="{Binding Settings.SelectedSize, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Grid VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                            <StackPanel Grid.Row="1" Orientation="Horizontal">
                                <TextBlock Text="{Binding Fit, Converter={StaticResource EnumValueConverter}, ConverterParameter=ThirdPersonSingular}" />
                                <TextBlock
                                    Margin="4,0,0,0"
                                    FontWeight="SemiBold"
                                    Text="{Binding Width, Converter={StaticResource AutoDoubleConverter}, ConverterParameter=Auto}" />
                                <TextBlock
                                    Margin="4,0,0,0"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="&#x00D7;"
                                    Visibility="{Binding ShowHeight, Converter={StaticResource BoolValueConverter}}" />
                                <TextBlock
                                    Margin="4,0,0,0"
                                    FontWeight="SemiBold"
                                    Text="{Binding Height, Converter={StaticResource AutoDoubleConverter}, ConverterParameter=Auto}"
                                    Visibility="{Binding ShowHeight, Converter={StaticResource BoolValueConverter}}" />
                                <TextBlock Margin="4,0,0,0" Text="{Binding Unit, Converter={StaticResource EnumValueConverter}, ConverterParameter=ToLower}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border
                Grid.RowSpan="5"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0" />

            <!-- AI Configuration Panel -->
            <Grid Margin="16">
                <!-- AI Model Download Prompt -->
                <StackPanel Visibility="{Binding ShowModelDownloadPrompt, Converter={StaticResource BoolValueConverter}}">
                    <InfoBar
                        IsClosable="False"
                        IsOpen="True"
                        Message="{Binding ModelStatusMessage}"
                        Severity="Informational" />

                    <Button
                        Margin="0,8,0,0"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource AccentButtonStyle}"
                        Command="{Binding DownloadModelCommand}"
                        Content="Download"
                        Visibility="{Binding IsModelDownloading, Converter={StaticResource BoolValueConverter}, ConverterParameter=Inverted}" />

                    <StackPanel Margin="0,8,0,0" Visibility="{Binding IsModelDownloading, Converter={StaticResource BoolValueConverter}}">
                        <ProgressRing IsActive="True" />
                        <TextBlock
                            Margin="0,8,0,0"
                            HorizontalAlignment="Center"
                            Text="{Binding ModelStatusMessage}" />
                    </StackPanel>
                </StackPanel>

                <!-- AI Scale Controls -->
                <StackPanel Visibility="{Binding ShowAiControls, Converter={StaticResource BoolValueConverter}}">
                    <Grid>
                        <TextBlock Text="Current:" />
                        <TextBlock HorizontalAlignment="Right" Text="{Binding AiScaleDisplay}" />
                    </Grid>

                    <Slider
                        Margin="0,8,0,0"
                        IsThumbToolTipEnabled="False"
                        SnapsTo="StepValues"
                        StepFrequency="1"
                        Maximum="8"
                        Minimum="1"
                        TickFrequency="1"
                        TickPlacement="Outside"
                        Value="{Binding AiSuperResolutionScale, Mode=TwoWay}" />

                    <StackPanel Margin="0,16,0,0" Visibility="{Binding ShowAiSizeDescriptions, Converter={StaticResource BoolValueConverter}}">
                        <Grid>
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Current:" />
                            <TextBlock
                                HorizontalAlignment="Right"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{Binding CurrentResolutionDescription}" />
                        </Grid>
                        <Grid Margin="0,8,0,0">
                            <TextBlock Text="New:" />
                            <TextBlock HorizontalAlignment="Right" Text="{Binding NewResolutionDescription}" />
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- "Custom" input matrix -->
            <Grid Margin="16" Visibility="{Binding ElementName=SizeComboBox, Path=SelectedValue, Converter={StaticResource SizeTypeToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <FontIcon
                    VerticalAlignment="Center"
                    FontSize="20"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE740;" />
                <NumberBox
                    Name="WidthNumberBox"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    HorizontalAlignment="Stretch"
                    KeyDown="Button_KeyDown"
                    Minimum="0"
                    SpinButtonPlacementMode="Inline"
                    NumberFormatter="{StaticResource ZeroToEmptyStringNumberFormatter}"
                    Value="{Binding ElementName=SizeComboBox, Path=SelectedValue.Width, Mode=TwoWay, Converter={StaticResource NumberBoxValueConverter}}" />

                <FontIcon
                    Grid.Column="3"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE744;"
                    Visibility="{Binding ElementName=SizeComboBox, Path=SelectedValue.ShowHeight, Converter={StaticResource BoolValueConverter}}" />
                <NumberBox
                    Name="HeightNumberBox"
                    Grid.Column="4"
                    Margin="8,0,0,0"
                    HorizontalAlignment="Stretch"
                    KeyDown="Button_KeyDown"
                    Minimum="0"
                    SpinButtonPlacementMode="Inline"
                    NumberFormatter="{StaticResource ZeroToEmptyStringNumberFormatter}"
                    Visibility="{Binding ElementName=SizeComboBox, Path=SelectedValue.ShowHeight, Converter={StaticResource BoolValueConverter}}"
                    Value="{Binding ElementName=SizeComboBox, Path=SelectedValue.Height, Mode=TwoWay, Converter={StaticResource NumberBoxValueConverter}}" />

                <FontIcon
                    Grid.Row="2"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE7A8;" />
                <ComboBox
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    HorizontalAlignment="Stretch"
                    ItemsSource="{Binding ResizeFitValues, Mode=OneWay}"
                    SelectedIndex="{Binding ElementName=SizeComboBox, Path=SelectedValue.Fit, Mode=TwoWay, Converter={StaticResource EnumToIntConverter}}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Padding="2,0" Text="{Binding Converter={StaticResource EnumValueConverter}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <FontIcon
                    Grid.Row="2"
                    Grid.Column="3"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE9B6;" />
                <ComboBox
                    Grid.Row="2"
                    Grid.Column="4"
                    Margin="8,0,0,0"
                    ItemsSource="{Binding ResizeUnitValues, Mode=OneWay}"
                    SelectedIndex="{Binding ElementName=SizeComboBox, Path=SelectedValue.Unit, Mode=TwoWay, Converter={StaticResource EnumToIntConverter}}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Padding="2,0" Text="{Binding Converter={StaticResource EnumValueConverter}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>

            <!-- CheckBoxes -->
            <StackPanel
                Grid.Row="1"
                Margin="16"
                Orientation="Vertical">
                <CheckBox Content="Make pictures smaller but not larger" IsChecked="{Binding Settings.ShrinkOnly, Mode=TwoWay}" />
                <CheckBox Content="Ignore the orientation of pictures" IsChecked="{Binding Settings.IgnoreOrientation, Mode=TwoWay}" />
                <CheckBox Content="Overwrite files" IsChecked="{Binding Settings.Replace, Mode=TwoWay}" />
                <CheckBox Content="Remove metadata that doesn't affect rendering" IsChecked="{Binding Settings.RemoveMetadata, Mode=TwoWay}" />
            </StackPanel>

            <!-- Separator line -->
            <Border
                Grid.Row="2"
                Height="1"
                Margin="0,8"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <InfoBar
                Grid.Row="3"
                Margin="16,0"
                IsClosable="False"
                IsOpen="{Binding TryingToResizeGifFiles}"
                Message="Gif files with animations may not be correctly resized."
                Severity="Warning" />

            <!-- Buttons -->
            <Grid Grid.Row="4" Margin="16,8,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    Padding="0"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Command="{Binding OpenSettingsCommand}"
                    ToolTipService.ToolTip="Settings">
                    <Button.Content>
                        <FontIcon FontSize="20" Glyph="&#xE713;" />
                    </Button.Content>
                </Button>

                <Button
                    Grid.Column="1"
                    MinWidth="76"
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{Binding ResizeCommand}">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontSize="16" Glyph="&#xE740;" />
                        <TextBlock Margin="8,0,0,0" Text="Resize" />
                    </StackPanel>
                </Button>

                <Button
                    Grid.Column="2"
                    MinWidth="76"
                    Margin="8,0,0,0"
                    Command="{Binding CancelCommand}"
                    Content="Cancel" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>
