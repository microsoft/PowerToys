<winuiex:WindowEx
    x:Class="TopToolbar.ToolbarWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d"
    x:Name="RootWindow"
    Activated="Window_Activated">
    <Border x:Name="ToolbarContainer" CornerRadius="36" Background="#F5F5F7" Opacity="0.96" Height="64" Margin="8" Padding="24,0" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="32" VerticalAlignment="Center">
                <!-- Group 1 -->
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <Button Content="🏠" ToolTipService.ToolTip="Home" Click="OnHomeClick" Style="{StaticResource AccentButtonStyle}" />
                    <Button Content="🔍" ToolTipService.ToolTip="Search" Click="OnSearchClick" />
                    <Button Content="📁" ToolTipService.ToolTip="Files" Click="OnFilesClick" />
                    <Button Content="🧮" ToolTipService.ToolTip="Calculator" Click="OnCalcClick" />
                </StackPanel>
                <Rectangle Width="1" Height="32" Fill="#33000000" VerticalAlignment="Center" />
                <!-- Group 2 -->
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <Button Content="📷" ToolTipService.ToolTip="Camera" Click="OnCameraClick" />
                    <Button Content="🎵" ToolTipService.ToolTip="Music" Click="OnMusicClick" />
                    <Button Content="✉" ToolTipService.ToolTip="Mail" Click="OnMailClick" />
                    <Button Content="📅" ToolTipService.ToolTip="Calendar" Click="OnCalendarClick" />
                </StackPanel>
                <Rectangle Width="1" Height="32" Fill="#33000000" VerticalAlignment="Center" />
                <!-- Group 3 -->
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <Button Content="💻" ToolTipService.ToolTip="Display" Click="OnDisplayClick" />
                    <Button Content="🔊" ToolTipService.ToolTip="Sound" Click="OnSoundClick" />
                    <Button Content="📶" ToolTipService.ToolTip="Network" Click="OnNetworkClick" />
                    <Button Content="⚙" ToolTipService.ToolTip="Settings" Click="OnSettingsClick" />
                </StackPanel>
            </StackPanel>
    </Border>
</winuiex:WindowEx>