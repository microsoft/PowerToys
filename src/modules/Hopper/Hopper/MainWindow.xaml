<Window x:Class="PowerToys.Hopper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PowerToys Hopper" Height="450" Width="800" AllowDrop="True" Drop="Window_Drop">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9*"/>
            <ColumnDefinition Width="71*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" x:Name="FileList" Grid.ColumnSpan="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>

        </ListView>
        <Border Margin="0,12,0,0"
                BorderThickness="0,1,0,0"
                Padding="12"
                Grid.Row="1" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Width="36"
                    Command="{Binding OpenSettingsCommand}"
                    Height="36"
                    Margin="-8,0,0,0"
                    Content="&#xE713;"
                    Background="Transparent"
                    FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                    HorizontalAlignment="Left" />

                <Button x:Name="CreateFolderButton"
                        Grid.Column="1"
                        MinWidth="76"
                        Margin="12,0,0,0"
                        Content="Create folder"
                        IsDefault="True" Click="CreateFolderButton_Click" />

                <Button Grid.Column="2"
                        MinWidth="76"
                        Margin="12,0,0,0"
                        Command="{Binding CancelCommand}"
                        Content="Cancel"
                        IsCancel="True"/>
            </Grid>
        </Border>
    </Grid>
</Window>
