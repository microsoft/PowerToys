<UserControl x:Class="RobocopyUI.Controls.PromptBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:animations="using:CommunityToolkit.WinUI.Animations"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:RobocopyUI.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="using:CommunityToolkit.WinUI"
             x:Name="PromptBoxControl"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <!--  Matches the Advanced Paste prompt so the AI entry point looks the same across modules.  -->
                <ResourceDictionary x:Key="Light">
                    <LinearGradientBrush x:Key="PromptFocusUnderlineBrush" StartPoint="0,0.5" EndPoint="1,0.5">
                        <GradientStop Offset="0.0" Color="#FF0078D4" />
                        <GradientStop Offset="0.42" Color="#FF464FEB" />
                        <GradientStop Offset="0.87" Color="#FFD660FF" />
                        <GradientStop Offset="1.0" Color="#FFFEA874" />
                    </LinearGradientBrush>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <LinearGradientBrush x:Key="PromptFocusUnderlineBrush" StartPoint="0,0.5" EndPoint="1,0.5">
                        <GradientStop Offset="0.0" Color="#FF0078D4" />
                        <GradientStop Offset="0.42" Color="#FF464FEB" />
                        <GradientStop Offset="0.87" Color="#FFD660FF" />
                        <GradientStop Offset="1.0" Color="#FFFEA874" />
                    </LinearGradientBrush>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <!--  Contrast themes have to stay on the system palette, so the brand gradient is
                          replaced by the system highlight colour.  -->
                    <SolidColorBrush x:Key="PromptFocusUnderlineBrush" Color="{ThemeResource SystemColorHighlightColor}" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <!--  A trimmed copy of the Advanced Paste prompt TextBox template: rounded, no delete
                  button, and a gradient underline while focused.  -->
            <Style x:Key="PromptTextBoxStyle" TargetType="TextBox">
                <Setter Property="Foreground" Value="{ThemeResource TextControlForeground}" />
                <Setter Property="Background" Value="{ThemeResource TextControlBackground}" />
                <Setter Property="BorderBrush" Value="{ThemeResource TextControlBorderBrush}" />
                <Setter Property="SelectionHighlightColor" Value="{ThemeResource TextControlSelectionHighlightColor}" />
                <Setter Property="BorderThickness" Value="{ThemeResource TextControlBorderThemeThickness}" />
                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
                <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
                <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Auto" />
                <Setter Property="ScrollViewer.VerticalScrollMode" Value="Auto" />
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
                <Setter Property="MinHeight" Value="{ThemeResource TextControlThemeMinHeight}" />
                <!--  Padding is what insets the text past the overlaid model selector and send
                      button, so the control keeps it in sync with the selector's measured width.  -->
                <Setter Property="Padding" Value="12,8,4,8" />
                <Setter Property="ContextFlyout" Value="{StaticResource TextControlCommandBarContextFlyout}" />
                <Setter Property="SelectionFlyout" Value="{StaticResource TextControlCommandBarSelectionFlyout}" />
                <Setter Property="CornerRadius" Value="16" />
                <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <!--  Reserves room for the send/cancel button overlaid by the control.  -->
                                    <ColumnDefinition Width="40" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter
                                    x:Name="HeaderContentPresenter"
                                    Grid.Row="0"
                                    Grid.ColumnSpan="2"
                                    Margin="{ThemeResource TextBoxTopHeaderMargin}"
                                    VerticalAlignment="Top"
                                    x:DeferLoadStrategy="Lazy"
                                    Content="{TemplateBinding Header}"
                                    ContentTemplate="{TemplateBinding HeaderTemplate}"
                                    FontWeight="Normal"
                                    Foreground="{ThemeResource TextControlHeaderForeground}"
                                    TextWrapping="Wrap"
                                    Visibility="Collapsed" />

                                <Border
                                    x:Name="BorderElement"
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    MinWidth="{TemplateBinding MinWidth}"
                                    MinHeight="{TemplateBinding MinHeight}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Control.IsTemplateFocusTarget="True"
                                    CornerRadius="{TemplateBinding CornerRadius}" />

                                <!--  Inset by the corner radius: a straight line any wider would run past
                                      the curve and hang outside the control.  -->
                                <Rectangle
                                    x:Name="FocusHighlighter"
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    Height="2"
                                    Margin="16,0,16,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Bottom"
                                    Fill="{ThemeResource PromptFocusUnderlineBrush}"
                                    RadiusX="1"
                                    RadiusY="1"
                                    Visibility="Collapsed" />

                                <ScrollViewer
                                    x:Name="ContentElement"
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Margin="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"
                                    AutomationProperties.AccessibilityView="Raw"
                                    Foreground="{TemplateBinding Foreground}"
                                    HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                    HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                    IsTabStop="False"
                                    VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                    VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                                    ZoomMode="Disabled" />

                                <TextBlock
                                    x:Name="PlaceholderTextContentPresenter"
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Margin="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"
                                    Foreground="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForeground}}"
                                    IsHitTestVisible="False"
                                    Text="{TemplateBinding PlaceholderText}"
                                    TextAlignment="{TemplateBinding TextAlignment}"
                                    TextWrapping="{TemplateBinding TextWrapping}" />

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundDisabled}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushDisabled}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundDisabled}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlPlaceholderForegroundDisabled}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <!--  Deliberately does not animate BorderBrush: the host signals an error by
                                                      setting BorderBrush on the TextBox, and an animated value here would
                                                      silently win over it for as long as the pointer is over the control.  -->
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundPointerOver}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Focused">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundFocused}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="FocusHighlighter" Storyboard.TargetProperty="Visibility">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="Visible" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundFocused}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="PromptBoxGrid" Loaded="Grid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!--  Reserved so swapping the footer for the loading caption never resizes the host.  -->
            <RowDefinition Height="Auto" MinHeight="24" />
        </Grid.RowDefinitions>

        <local:AnimatedContentControl
            x:Name="Loader"
            MinHeight="48"
            CornerRadius="16">
            <Grid>
                <TextBox
                    x:Name="InputTxtBox"
                    x:Uid="PromptBoxInput"
                    MaxHeight="120"
                    HorizontalAlignment="Stretch"
                    AcceptsReturn="True"
                    IsSpellCheckEnabled="True"
                    KeyDown="InputTxtBox_KeyDown"
                    PlaceholderText="{x:Bind PlaceholderText, Mode=OneWay}"
                    Style="{StaticResource PromptTextBoxStyle}"
                    TabIndex="1"
                    Text="{x:Bind Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap" />

                <!--  Sits in the gutter the input reserves through its left padding. Bottom aligned on the
                      same baseline as the send button, so the two stay level as the input grows, and
                      inset by the same amount so they are centred at the single-line height.
                      A ContentControl rather than a ContentPresenter so the loading state can disable it.  -->
                <ContentControl
                    x:Name="ModelSelectorPresenter"
                    Margin="8,0,0,8"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Content="{x:Bind ModelSelector, Mode=OneWay}"
                    IsTabStop="False"
                    SizeChanged="ModelSelectorPresenter_SizeChanged" />

                <!--  Mirrors the model selector's inset so the two sit on one baseline, clear of the
                      rounded corner, and centred while the input is a single line.  -->
                <Grid
                    Width="32"
                    Height="32"
                    Margin="0,0,8,8"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
                    <Button
                        x:Name="SendBtn"
                        x:Uid="PromptBoxSendButton"
                        Padding="0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        ui:VisualExtensions.NormalizedCenterPoint="0.5,0.5"
                        Click="SendBtn_Click"
                        Content="{ui:FontIcon Glyph=&#xE724;,
                                              FontSize=16}"
                        Style="{StaticResource SubtleButtonStyle}"
                        TabIndex="2">
                        <animations:Implicit.ShowAnimations>
                            <animations:ScaleAnimation
                                From="0.4"
                                To="1"
                                Duration="0:0:0.167" />
                            <animations:OpacityAnimation
                                From="0.0"
                                To="1.0"
                                Duration="0:0:0.167" />
                        </animations:Implicit.ShowAnimations>
                        <animations:Implicit.HideAnimations>
                            <animations:ScaleAnimation
                                From="1"
                                To="0.4"
                                Duration="0:0:0.167" />
                            <animations:OpacityAnimation
                                From="1.0"
                                To="0.0"
                                Duration="0:0:0.167" />
                        </animations:Implicit.HideAnimations>
                    </Button>

                    <Button
                        x:Name="CancelBtn"
                        x:Uid="PromptBoxCancelButton"
                        Padding="0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        ui:VisualExtensions.NormalizedCenterPoint="0.5,0.5"
                        Click="CancelBtn_Click"
                        Content="{ui:FontIcon Glyph=&#xE711;,
                                              FontSize=16}"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        IsEnabled="False"
                        Style="{StaticResource SubtleButtonStyle}"
                        Visibility="Collapsed" />
                </Grid>
            </Grid>
        </local:AnimatedContentControl>

        <!--  Flush with the input's left edge, matching how a TextBox header and description line up
              with the field they describe.  -->
        <ContentPresenter
            x:Name="FooterPresenter"
            Grid.Row="1"
            Margin="0,6,0,0"
            VerticalAlignment="Top"
            Content="{x:Bind Footer, Mode=OneWay}" />

        <TextBlock
            x:Name="LoadingText"
            x:Uid="PromptBoxLoadingText"
            Grid.Row="1"
            Margin="0,6,0,0"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
            Visibility="Collapsed">
            <animations:Implicit.ShowAnimations>
                <animations:OpacityAnimation To="1.0" Duration="0:0:0.6" />
            </animations:Implicit.ShowAnimations>
            <animations:Implicit.HideAnimations>
                <animations:OpacityAnimation To="0.0" Duration="0:0:0.167" />
            </animations:Implicit.HideAnimations>
        </TextBlock>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="DefaultState" />
                <VisualState x:Name="LoadingState">
                    <VisualState.Setters>
                        <Setter Target="Loader.IsLoading" Value="True" />
                        <Setter Target="InputTxtBox.IsEnabled" Value="False" />
                        <Setter Target="SendBtn.IsEnabled" Value="False" />
                        <Setter Target="SendBtn.Visibility" Value="Collapsed" />
                        <Setter Target="CancelBtn.IsEnabled" Value="True" />
                        <Setter Target="CancelBtn.Visibility" Value="Visible" />
                        <!--  Disabled rather than collapsed: collapsing it would drop the input's left
                              gutter and shift the text mid-request.  -->
                        <Setter Target="ModelSelectorPresenter.IsEnabled" Value="False" />
                        <Setter Target="FooterPresenter.Visibility" Value="Collapsed" />
                        <Setter Target="LoadingText.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="ErrorState">
                    <VisualState.Setters>
                        <Setter Target="InputTxtBox.BorderBrush" Value="{ThemeResource SystemFillColorCriticalBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</UserControl>
