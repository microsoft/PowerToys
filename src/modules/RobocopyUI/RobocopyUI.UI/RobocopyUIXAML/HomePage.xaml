<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="RobocopyUI.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RobocopyUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:RobocopyUI.Controls"
    xmlns:models="using:RobocopyUI.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="OptionDataTemplate" x:DataType="models:OptionContent">
            <controls:OptionEntry
                Loaded="OptionEntry_Loaded"
                Unloaded="OptionEntry_Unloaded"
                OptionName="{x:Bind OptionName}"
                OptionDescription="{x:Bind OptionDescription}"
                IsNumberOption="{x:Bind IsNumberOption}"
                IsMultiSelectOption="{x:Bind IsMultiSelectOption}"
                MultiSelectOptions="{x:Bind MultiSelectOptions}"
                IsRunHoursOption="{x:Bind IsRunHoursOption}"
                IsStorageOption="{x:Bind IsStorageOption}"
                IsTextOption="{x:Bind IsTextOption}" />
        </DataTemplate>

        <DataTemplate x:Key="OptionGroupStyleHeaderTemplate">
            <TextBlock Text="{Binding Key}" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,12,0,4" />
        </DataTemplate>

        <GroupStyle x:Key="OptionsGroupStyle" HeaderTemplate="{StaticResource OptionGroupStyleHeaderTemplate}"/>
    </Page.Resources>

    <StackPanel x:Name="TopPanelStack"  Margin="12">
        <TextBlock Margin="4" x:Uid="HomePageTitle" Style="{ThemeResource TitleLargeTextBlockStyle}" />
        <StackPanel Margin="0,12,0,12">
            <Grid Margin="12,0,12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock x:Uid="SourceLabel" Grid.Column="0" Grid.Row="0" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock x:Uid="DestinationLabel" Grid.Column="3" Grid.Row="0" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBox Margin="0,0,4,0" x:Name="SourceTextBox" Grid.Column="0" Grid.Row="1" TextChanged="SourceTextBox_TextChanged" />
                <Button Margin="0,0,4,0" x:Uid="SourceBrowseButton" x:Name="SourceBrowseButton" Grid.Column="1" Grid.Row="1" Click="SourceBrowseButton_Click">
                    <FontIcon Glyph="&#xE8B7;" />
                </Button>
                <Button Margin="4,0,4,0" x:Uid="SwapButton" x:Name="SwapButton" Grid.Column="2" Grid.Row="1" Click="SwapButton_Click">
                    <FontIcon Glyph="&#xE8AB;" />
                </Button>
                <TextBox Margin="4,0,4,0" x:Name="DestinationTextBox" Grid.Column="3" Grid.Row="1" TextChanged="SourceTextBox_TextChanged" />
                <Button Margin="0,0,0,0" x:Uid="DestinationBrowseButton" Grid.Column="4" Grid.Row="1" x:Name="DestinationBrowseButton" Click="DestinationBrowseButton_Click">
                    <FontIcon Glyph="&#xE8B7;" />
                </Button>
            </Grid>

            <Grid x:Name="CommandPreviewContent" Margin="12" ColumnSpacing="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="CommandPreviewTextBox" x:Uid="CommandPreviewTextBox" Width="Auto" Height="Auto" IsReadOnly="True" TextWrapping="Wrap" />

                <Button Click="LoadButton_Click" x:Name="LoadButton" Grid.Column="1" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE8E5;" />
                        <TextBlock Margin="4,0,0,0" x:Uid="LoadButtonLabel" />
                    </StackPanel>
                </Button>

                <Button Click="SaveButton_Click" x:Name="SaveButton" Grid.Column="2" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE78C;" />
                        <TextBlock Margin="4,0,0,0" x:Uid="SaveButtonLabel"/>
                    </StackPanel>
                </Button>
                
                <Button Style="{StaticResource AccentButtonStyle}" Click="RunButton_Click" x:Name="RunButton" IsEnabled="False" Grid.Column="3" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE768;" />
                        <TextBlock Margin="4,0,0,0" x:Uid="RunButtonLabel" Foreground="White" />
                    </StackPanel>
                </Button>
            </Grid>

            <SelectorBar SelectionChanged="SelectorBar_SelectionChanged">
                <SelectorBar.Resources>
                    <x:Double x:Key="SelectorBarItemPillWidth">15</x:Double>
                </SelectorBar.Resources>
                <SelectorBarItem x:Uid="OptionsSelectorBarLabel" Tag="Options" IsSelected="True">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE762;" />
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem x:Uid="FiltersSelectorBarLabel" Tag="Filters">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE71C;" />
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem x:Uid="LoggingSelectorBarLabel" Tag="Logging">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE9F9;" />
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem x:Uid="AdvancedSelectorBarLabel" Tag="Advanced">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
                <SelectorBarItem x:Uid="OutputSelectorBarLabel" Tag="Output" x:Name="OutputSelectorBarItem" IsEnabled="False">
                    <SelectorBarItem.Icon>
                        <FontIcon Glyph="&#xE768;" />
                    </SelectorBarItem.Icon>
                </SelectorBarItem>
            </SelectorBar>

            <Grid x:Name="OptionsContent" Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ListView x:Name="OptionsListView" ItemTemplate="{StaticResource OptionDataTemplate}" SelectionMode="None" IsItemClickEnabled="False" Grid.Column="0">
                    <ListView.GroupStyle>
                        <GroupStyle HeaderTemplate="{StaticResource OptionGroupStyleHeaderTemplate}"/>
                    </ListView.GroupStyle>
                </ListView>
                <ListView x:Name="OptionsListViewRight"  ItemTemplate="{StaticResource OptionDataTemplate}" SelectionMode="None" IsItemClickEnabled="False" Grid.Column="1" >
                    <ListView.GroupStyle>
                        <GroupStyle HeaderTemplate="{StaticResource OptionGroupStyleHeaderTemplate}"/>
                    </ListView.GroupStyle>
                </ListView>
            </Grid>
            <Grid x:Name="FiltersContent" Margin="12">
                <ListView x:Name="FilterOptionsListView" ItemTemplate="{StaticResource OptionDataTemplate}" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.GroupStyle>
                        <GroupStyle HeaderTemplate="{StaticResource OptionGroupStyleHeaderTemplate}"/>
                    </ListView.GroupStyle>
                </ListView>
            </Grid>
            <Grid x:Name="LoggingContent" Margin="12">
                <ListView x:Name="LoggingOptionsListView" ItemTemplate="{StaticResource OptionDataTemplate}" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.GroupStyle>
                        <GroupStyle HeaderTemplate="{StaticResource OptionGroupStyleHeaderTemplate}"/>
                    </ListView.GroupStyle>
                </ListView>
            </Grid>
            <Grid x:Name="AdvancedContent" Margin="12">
                <ListView x:Name="AdvancedOptionsListView" ItemTemplate="{StaticResource OptionDataTemplate}" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.GroupStyle>
                        <GroupStyle HeaderTemplate="{StaticResource OptionGroupStyleHeaderTemplate}"/>
                    </ListView.GroupStyle>
                </ListView>
            </Grid>
            <StackPanel x:Name="OutputContent" Margin="12" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Margin="4">
                    <TextBlock x:Uid="OutputStatusLabel" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock>&#160;</TextBlock>
                    <TextBlock x:Name="OutputStatusText" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock>&#160;</TextBlock>
                    <TextBlock>(</TextBlock>
                    <TextBlock x:Uid="StatusCodeLabel" />
                    <TextBlock>&#160;</TextBlock>
                    <TextBlock x:Name="StatusCodeText" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock>)</TextBlock>
                </StackPanel>
                <TextBox x:Name="OutputTextBox" Width="Auto" MinHeight="200" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" FontFamily="Cascadia Mono" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>
