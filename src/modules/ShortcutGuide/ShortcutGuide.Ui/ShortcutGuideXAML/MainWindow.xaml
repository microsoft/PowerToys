<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx
    x:Class="ShortcutGuide.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    IsMaximizable="False"
    IsMinimizable="False"
    IsResizable="true"
    IsShownInSwitchers="False"
    mc:Ignorable="d">
    <winuiex:WindowEx.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </winuiex:WindowEx.SystemBackdrop>
    <Page x:Name="MainPage">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TitleBar Title="Shortcut Guide">
                <TitleBar.Content>
                    <TextBox
                        x:Name="SearchBox"
                        x:Uid="SearchBox"
                        Width="300"
                        VerticalAlignment="Center"
                        TextChanged="SearchBox_TextChanged">
                        <TextBox.KeyboardAccelerators>
                            <KeyboardAccelerator
                                Key="F"
                                Invoked="SearchBox_KeyboardAcceleratorInvoked"
                                Modifiers="Control" />
                        </TextBox.KeyboardAccelerators>
                    </TextBox>
                </TitleBar.Content>
                <TitleBar.RightHeader>
                    <Button x:Uid="CloseButton" Click="CloseButton_Clicked">
                        <FontIcon FontSize="14" Glyph="&#xE711;" />
                    </Button>
                </TitleBar.RightHeader>
            </TitleBar>

            <NavigationView
                x:Name="WindowSelector"
                Grid.Row="1"
                IsBackButtonVisible="Collapsed"
                IsSettingsVisible="True"
                SelectionChanged="WindowSelector_SelectionChanged"
                Style="{StaticResource RailNavigationViewStyle}">
                <NavigationView.MenuItems />
                <Frame
                    x:Name="ContentFrame"
                    Grid.Row="1"
                    Grid.ColumnSpan="5" />
                <NavigationView.Resources>
                    <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                    <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                </NavigationView.Resources>
            </NavigationView>

            <!--<Button
                x:Name="InformationButton"
                x:Uid="InformationButton"
                Grid.Column="1"
                Margin="0,0,6,0"
                Click="InformationButton_Click">
                <FontIcon FontSize="14" Glyph="&#xEA1F;" />
            </Button>-->
            <!--<TeachingTip
                x:Name="InformationTip"
                x:Uid="InformationTip"
                Target="{x:Bind InformationButton}">
                <StackPanel>
                    <TextBlock
                        x:Uid="InformationTip1"
                        Margin="0,16,0,0"
                        TextWrapping="WrapWholeWords" />
                    <TextBlock
                        x:Uid="InformationTip2"
                        Margin="0,16,0,0"
                        TextWrapping="WrapWholeWords" />
                    <HyperlinkButton
                        x:Uid="ShowOOBE"
                        Margin="-8,0,0,0"
                        Click="HyperlinkButton_Click" />
                </StackPanel>
            </TeachingTip>-->

            <!--<Button
                x:Uid="SettingsButton"
                Grid.Column="3"
                Margin="0,0,6,0"
                HorizontalContentAlignment="Center"
                Click="SettingsButton_Clicked">
                <FontIcon FontSize="14" Glyph="&#xE713;" />
            </Button>-->


        </Grid>
    </Page>
</winuiex:WindowEx>
