<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ShortcutGuide.Pages.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ShortcutGuide"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ShortcutGuide.Models"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="ShortcutItemTemplate" x:DataType="models:ShortcutEntry">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Orientation="Vertical">
                    <TextBlock Text="{x:Bind Name}" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind Description}" />
                </StackPanel>
                <ItemsControl Grid.Column="1" ItemsSource="{x:Bind Shortcut}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="4" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid
                                Padding="12,8,12,8"
                                Background="{ThemeResource ControlSolidFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource ControlCornerRadius}">
                                <TextBlock FontWeight="SemiBold" Text="Alt" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <StackPanel
            Margin="0,16,0,0"
            Orientation="Vertical"
            Spacing="8">
            <TextBlock
                x:Name="RecommendedHeaderTxt"
                Margin="16,0,0,8"
                FontWeight="SemiBold"
                Text="Recommended shortcuts" />
            <ListView
                ItemTemplate="{StaticResource ShortcutItemTemplate}"
                ItemsSource="{x:Bind _recommendedShortcuts, Mode=OneWay}"
                SelectionMode="None"
                Visibility="{x:Bind _recommendedShortcuts.Count, Mode=OneWay, Converter={StaticResource DoubleToVisibilityConverter}}" />

            <TextBlock
                Margin="16,0,0,8"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="No recommended shortcuts."
                Visibility="{x:Bind _recommendedShortcuts.Count, Mode=OneWay, Converter={StaticResource DoubleToInvertedVisibilityConverter}}" />

            <TextBlock
                x:Name="PinnedHeaderTxt"
                Margin="16,16,0,8"
                FontWeight="SemiBold"
                Text="Pinned shortcuts" />

            <ListView
                ItemTemplate="{StaticResource ShortcutItemTemplate}"
                ItemsSource="{x:Bind _pinnedShortcuts, Mode=OneWay}"
                SelectionMode="None"
                Visibility="{x:Bind _pinnedShortcuts.Count, Mode=OneWay, Converter={StaticResource DoubleToVisibilityConverter}}" />

            <TextBlock
                Margin="16,0,0,8"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="No recommended shortcuts."
                Visibility="{x:Bind _pinnedShortcuts.Count, Mode=OneWay, Converter={StaticResource DoubleToInvertedVisibilityConverter}}" />

        </StackPanel>
    </Grid>
</Page>
