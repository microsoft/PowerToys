<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="ShortcutGuide.Controls.MainPaneControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Common.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!--
        Pseudo-window chrome (per-card desktop acrylic + 1px border + 8px corner
        radius + ThemeShadow) is provided by the shared TransientSurface from
        Common.UI.Controls instead of being hand-rolled here. Its built-in
        show/hide transitions are intentionally left at the default (None):
        OverlayWindow drives the entrance/exit animation on this whole control
        (slide + fade keyed to the user's left/right alignment), so the surface
        only contributes chrome here.
    -->
    <controls:TransientSurface AcrylicKind="Thin" Visibility="Visible">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Padding="16,0" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image
                    Width="16"
                    Height="16"
                    VerticalAlignment="Center"
                    Source="/Assets/ShortcutGuide/ShortcutGuide.ico" />
                <TextBlock
                    x:Name="TitleTextBlock"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}" />
                <Button
                    x:Name="CloseButton"
                    x:Uid="CloseButton"
                    Grid.Column="2"
                    Width="32"
                    Height="32"
                    Padding="0"
                    VerticalAlignment="Center"
                    Click="CloseButton_Click"
                    Style="{StaticResource SubtleButtonStyle}">
                    <FontIcon FontSize="12" Glyph="&#xE711;" />
                </Button>
            </Grid>

            <NavigationView
                x:Name="WindowSelector"
                Grid.Row="1"
                IsBackButtonVisible="Collapsed"
                IsPaneToggleButtonVisible="False"
                IsSettingsVisible="False"
                SelectionChanged="WindowSelector_SelectionChanged"
                Style="{StaticResource RailNavigationViewStyle}">
                <NavigationView.MenuItems />
                <NavigationView.FooterMenuItems>
                    <NavigationViewItem
                        x:Uid="SettingsButton"
                        Icon="Setting"
                        SelectsOnInvoked="False"
                        Tag="Settings"
                        Tapped="Settings_Tapped" />
                </NavigationView.FooterMenuItems>
                <NavigationView.Content>
                    <Frame
                        x:Name="ContentFrame"
                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1,1,0,0"
                        CornerRadius="8,0,0,0" />
                </NavigationView.Content>
                <NavigationView.Resources>
                    <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                    <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                </NavigationView.Resources>
            </NavigationView>
        </Grid>
    </controls:TransientSurface>
</UserControl>
