<UserControl
    x:Class="Microsoft.PowerToys.Settings.UI.Controls.HotkeySettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
   x:Name="LayoutRoot"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        
   
    <StackPanel Orientation="Vertical">
        
   
    <TextBox x:Name="HotkeyTextBox"
             x:Uid="SettingsPage_SetShortcut"
             Visibility="Collapsed"
             IsReadOnly="True"/>
        <ItemsControl x:Name="PreviewKeysControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:KeyVisual Content="{Binding}" Height="36" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="Open dialog" x:Name="OpenDialogButton" Click="OpenDialogButton_Click" />

    </StackPanel>
        <ContentDialog x:Name="ShortcutDialog"
                       Title="Activation shortcut"
                       PrimaryButtonText="Save"
                       CloseButtonText="Cancel"
                       HorizontalAlignment="Stretch"
                       HorizontalContentAlignment="Stretch"
                       PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
                       PrimaryButtonClick="ShortcutDialog_PrimaryButtonClick"
                       Closing="ShortcutDialog_Closing"
                       Opened="ShortcutDialog_Opened">
            <StackPanel Orientation="Vertical" Spacing="12" Width="500" Height="200">
                <TextBlock TextWrapping="WrapWholeWords">
                        <Run Text="Only shortcuts with the with the following hotkeys are valid:"/><LineBreak/>
                        <Run Text="Windows key, Ctrl, Alt and Shift"/>
                </TextBlock>
                <ItemsControl x:Name="KeysControl" HorizontalAlignment="Center" Margin="0,12,0,12">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:KeyVisual AutomationProperties.AccessibilityView="Raw" Height="56" IsTabStop="False" Content="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <muxc:InfoBar x:Name="WarningMessage" Severity="Error" Title="Only shortcuts with the with the following hotkeys are valid: Windows key, Ctrl, Alt and Shift." IsOpen="False" IsClosable="False" />
            </StackPanel>
        </ContentDialog>
    </Grid>
</UserControl>