<Page
    x:Class="Microsoft.PowerToys.Settings.UI.OOBE.Views.OobeOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.OOBE.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Orientation="Vertical"
                    VerticalAlignment="Top">
            <Image Stretch="UniformToFill"
                   Height="320"
                   Source="{x:Bind ViewModel.PreviewImageSource}" />
            <TextBlock Text="{x:Bind ViewModel.ModuleName}"
                       FontWeight="SemiBold"
                       FontSize="32"
                       Margin="32,32,32,16" />
            <TextBlock Margin="32,0,32,16"
                       TextWrapping="Wrap"
                       Opacity="0.8" >
                <Run x:Uid="Oobe_Overview_Description" />
                <Hyperlink NavigateUri="{x:Bind ViewModel.DescriptionLink}" Foreground="{ThemeResource SystemControlBackgroundAccentBrush}">
                    <Run x:Uid="Oobe_Overview_DescriptionLinkText" />
                </Hyperlink>
            </TextBlock>
            <TextBlock Margin="32,0,32,0"
                       TextWrapping="Wrap"
                       Opacity="0.8" >
                <Run x:Uid="Oobe_Overview_CheckoutLatestVersion" />
                <Hyperlink NavigateUri="{x:Bind ViewModel.Link}" >
                    <Run x:Uid="Oobe_Overview_LatestVersionLink" />
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>
