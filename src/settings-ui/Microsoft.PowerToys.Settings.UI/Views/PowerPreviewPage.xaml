<Page x:Class="Microsoft.PowerToys.Settings.UI.Views.PowerPreviewPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converters="using:Microsoft.Toolkit.Uwp.UI.Converters" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      AutomationProperties.LandmarkType="Main"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToObjectConverter x:Key="BoolToVisibilityConverter" FalseValue="Visible" TrueValue="Collapsed" />
    </Page.Resources>

    <Grid RowSpacing="{StaticResource DefaultRowSpacing}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel x:Name="PowerPreviewView"
                    MaxWidth="{StaticResource MaxContentWidth}"
                    Margin="0,0,48,0" HorizontalAlignment="Left" Orientation="Vertical">
            <TextBlock x:Uid="FileExplorerPreview_RunAsAdminRequired"
                       Margin="{StaticResource SmallBottomMargin}"
                       Foreground="{ThemeResource SystemControlErrorTextForegroundBrush}"
                       TextWrapping="Wrap"
                       Visibility="{Binding Mode=OneWay, Path=IsElevated, Converter={StaticResource BoolToVisibilityConverter}}" />

            <TextBlock x:Uid="FileExplorerPreview_AffectsAllUsers"
                       Margin="{StaticResource SmallBottomMargin}"
                       FontWeight="SemiBold"
                       Foreground="{x:Bind Mode=OneWay, Path=ViewModel.IsElevated, Converter={StaticResource ModuleEnabledToForegroundConverter}}"
                       TextWrapping="Wrap" />

            <TextBlock x:Uid="FileExplorerPreview_PreviewPane_GroupSettings"
                       Foreground="{x:Bind Mode=OneWay, Path=ViewModel.IsElevated, Converter={StaticResource ModuleEnabledToForegroundConverter}}"
                       Style="{StaticResource SettingsGroupTitleStyleAsHeader}" />

            <ToggleSwitch x:Uid="FileExplorerPreview_ToggleSwitch_Preview_SVG"
                          Margin="{StaticResource SmallTopMargin}"
                          IsEnabled="{Binding Mode=OneWay, Path=IsElevated}"
                          IsOn="{Binding Mode=TwoWay, Path=SVGRenderIsEnabled}" />

            <ToggleSwitch x:Uid="FileExplorerPreview_ToggleSwitch_Preview_MD"
                          Margin="{StaticResource SmallTopMargin}"
                          IsEnabled="{Binding Mode=OneWay, Path=IsElevated}"
                          IsOn="{Binding Mode=TwoWay, Path=MDRenderIsEnabled}" />

            <ToggleSwitch x:Uid="FileExplorerPreview_ToggleSwitch_Preview_PDF"
                          Margin="{StaticResource SmallTopMargin}"
                          IsEnabled="{Binding Mode=OneWay, Path=IsElevated}"
                          IsOn="{Binding Mode=TwoWay, Path=PDFRenderIsEnabled}" />

            <TextBlock x:Uid="FileExplorerPreview_IconThumbnail_GroupSettings"
                       Foreground="{x:Bind Mode=OneWay, Path=ViewModel.IsElevated, Converter={StaticResource ModuleEnabledToForegroundConverter}}"
                       Style="{StaticResource SettingsGroupTitleStyle}" />

            <TextBlock x:Uid="FileExplorerPreview_RebootRequired"
                       Margin="{StaticResource SmallTopBottomMargin}"
                       FontWeight="SemiBold"
                       Foreground="{x:Bind Mode=OneWay, Path=ViewModel.IsElevated, Converter={StaticResource ModuleEnabledToForegroundConverter}}"
                       TextWrapping="Wrap" />

            <ToggleSwitch x:Uid="FileExplorerPreview_ToggleSwitch_SVG_Thumbnail"
                          Margin="{StaticResource SmallTopMargin}"
                          IsEnabled="{Binding Mode=OneWay, Path=IsElevated}"
                          IsOn="{Binding Mode=TwoWay, Path=SVGThumbnailIsEnabled}" />
        </StackPanel>
        <RelativePanel x:Name="SidePanel" Grid.Column="1"
                       Width="{StaticResource SidePanelWidth}"
                       HorizontalAlignment="Left">
            <StackPanel x:Name="DescriptionPanel">
                <TextBlock x:Name="AboutTitle" x:Uid="About_FileExplorerPreview" Grid.ColumnSpan="2"
                           Margin="{StaticResource XSmallBottomMargin}"
                           Style="{StaticResource SettingsGroupTitleStyle}" />
                <TextBlock x:Uid="FileExplorerPreview_Description" Grid.Row="1" TextWrapping="Wrap" />
            </StackPanel>

            <Border x:Name="AboutImage" Grid.Row="2" MaxWidth="240"
                    Margin="{StaticResource SmallTopBottomMargin}"
                    HorizontalAlignment="Left" CornerRadius="4" RelativePanel.Below="DescriptionPanel">
                <HyperlinkButton x:Uid="FileExplorerPreview_ImageHyperlinkToDocs">
                    <Image x:Uid="FileExplorerPreview_Image" Source="ms-appx:///Assets/Modules/PowerPreview.png" />
                </HyperlinkButton>
            </Border>

            <StackPanel x:Name="LinksPanel" Margin="0,1,0,0" Orientation="Vertical"
                        RelativePanel.Below="AboutImage">
                <HyperlinkButton x:Uid="FileExplorerPreview_ImageHyperlinkToDocs">
                    <TextBlock x:Uid="Module_overview" />
                </HyperlinkButton>
                <HyperlinkButton NavigateUri="https://github.com/microsoft/PowerToys/issues">
                    <TextBlock x:Uid="Give_Feedback" />
                </HyperlinkButton>
            </StackPanel>
        </RelativePanel>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutVisualStates">
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideLayoutMinWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="SmallLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource SmallLayoutMinWidth}" />
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SidePanel.(Grid.Column)" Value="0" />
                        <Setter Target="SidePanel.Width" Value="Auto" />
                        <Setter Target="PowerPreviewView.(Grid.Row)" Value="1" />
                        <Setter Target="PowerPreviewView.Margin" Value="0" />
                        <Setter Target="LinksPanel.(RelativePanel.RightOf)" Value="AboutImage" />
                        <Setter Target="LinksPanel.(RelativePanel.AlignTopWith)" Value="AboutImage" />
                        <Setter Target="AboutImage.Margin" Value="0,12,12,0" />
                        <Setter Target="AboutTitle.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>