<Page
    x:Class="Microsoft.PowerToys.Settings.UI.OOBE.Views.OobeOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <ImageSource x:Key="HeroImageUrl">ms-appx:///Assets/Settings/Modules/OOBE/pt-hero.dark.png</ImageSource>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <ImageSource x:Key="HeroImageUrl">ms-appx:///Assets/Settings/Modules/OOBE/pt-hero.light.png</ImageSource>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <ImageSource x:Key="HeroImageUrl">ms-appx:///Assets/Settings/Modules/OOBE/pt-hero.dark.png</ImageSource>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Style="{StaticResource PageStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image
            x:Name="HeaderImage"
            Height="280"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Source="{ThemeResource HeroImageUrl}"
            Stretch="UniformToFill" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="32,0,32,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel
                    VerticalAlignment="Top"
                    Orientation="Vertical"
                    Spacing="12">

                    <TextBlock
                        x:Uid="Oobe_Overview_Title"
                        HorizontalAlignment="Center"
                        AutomationProperties.HeadingLevel="Level1"
                        Style="{StaticResource TitleTextBlockStyle}" />

                    <TextBlock
                        x:Uid="Oobe_Overview_Description"
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        TextAlignment="Center"
                        TextWrapping="Wrap" />

                    <StackPanel
                        Margin="0,24,0,48"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="16">
                        <Button
                            x:Uid="Oobe_Overview_ConfigurePowerToysButton"
                            VerticalAlignment="Center"
                            Click="SettingsLaunchButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon FontSize="14" Glyph="&#xE713;" />
                                <TextBlock
                                    x:Uid="Oobe_Overview_ConfigurePowerToysButton_Text"
                                    Grid.Row="1"
                                    Margin="0,-1,0,0"
                                    HorizontalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <HyperlinkButton
                            VerticalAlignment="Center"
                            NavigateUri="https://aka.ms/PowerToysOverview"
                            Style="{StaticResource TextButtonStyle}">
                            <TextBlock x:Uid="Oobe_Overview_DescriptionLinkText" TextWrapping="Wrap" />
                        </HyperlinkButton>
                    </StackPanel>

                </StackPanel>
                <tkcontrols:SettingsCard
                    x:Uid="Oobe_Overview_EnableDataDiagnostics"
                    Grid.Row="1"
                    Background="Transparent"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE9D9;}"
                    IsEnabled="{x:Bind ShowDataDiagnosticsSetting, Mode=OneWay}"
                    Visibility="{x:Bind ShowDataDiagnosticsSetting, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <tkcontrols:SettingsCard.Description>
                        <StackPanel Orientation="Vertical">
                            <TextBlock
                                x:Uid="GeneralPage_EnableDataDiagnosticsText"
                                Style="{StaticResource SecondaryTextStyle}"
                                TextWrapping="WrapWholeWords" />
                            <HyperlinkButton
                                x:Uid="GeneralPage_DiagnosticsAndFeedback_Link"
                                Margin="0,2,0,0"
                                FontWeight="SemiBold"
                                NavigateUri="https://aka.ms/powertoys-data-and-privacy-documentation" />
                            <HyperlinkButton
                                x:Uid="Oobe_Overview_DiagnosticsAndFeedback_Settings_Link"
                                Margin="0,2,0,0"
                                Click="GeneralSettingsLaunchButton_Click"
                                FontWeight="SemiBold" />
                        </StackPanel>
                    </tkcontrols:SettingsCard.Description>
                    <ToggleSwitch x:Uid="ToggleSwitch" IsOn="{x:Bind EnableDataDiagnostics, Mode=TwoWay}" />
                </tkcontrols:SettingsCard>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>