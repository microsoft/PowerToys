<local:NavigablePage
    x:Class="Microsoft.PowerToys.Settings.UI.Views.RobocopyUIPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <controls:SettingsPageControl x:Uid="RobocopyUI" ModuleImageSource="ms-appx:///Assets/Settings/Modules/RobocopyUI.png">
        <controls:SettingsPageControl.ModuleContent>
            <StackPanel ChildrenTransitions="{StaticResource SettingsCardsAnimations}" Orientation="Vertical">
                <controls:GPOInfoControl ShowWarning="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay}">
                    <tkcontrols:SettingsCard
                        Name="RobocopyUIEnableRobocopyUI"
                        x:Uid="RobocopyUI_Enable_RobocopyUI"
                        HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/RobocopyUI.png}">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.IsRobocopyUIEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </tkcontrols:SettingsCard>
                </controls:GPOInfoControl>
                <controls:SettingsGroup x:Uid="RobocopyUI_Launch_GroupSettings" IsEnabled="{x:Bind ViewModel.IsRobocopyUIEnabled, Mode=OneWay}">
                    <tkcontrols:SettingsCard
                        Name="RobocopyUILaunchButtonControl"
                        x:Uid="RobocopyUI_LaunchButtonControl"
                        ActionIcon="{ui:FontIcon Glyph=&#xE8A7;}"
                        Command="{x:Bind ViewModel.LaunchEventHandler}"
                        HeaderIcon="{ui:FontIcon Glyph=&#xEA37;}"
                        IsClickEnabled="True" />
                </controls:SettingsGroup>
                <controls:SettingsGroup x:Uid="RobocopyUI_AI_GroupSettings" IsEnabled="{x:Bind ViewModel.IsRobocopyUIEnabled, Mode=OneWay}">
                    <InfoBar x:Uid="RobocopyUI_AI_InfoBar" IsOpen="True" IsClosable="False">
                        <InfoBar.ActionButton>
                            <HyperlinkButton x:Uid="RobocopyUI_AI_HyperlinkButton" Click="HyperlinkButton_Click" />
                        </InfoBar.ActionButton>
                    </InfoBar>
                </controls:SettingsGroup>
                <controls:SettingsGroup x:Uid="RobocopyUI_Behaviour_GroupSettings" IsEnabled="{x:Bind ViewModel.IsRobocopyUIEnabled, Mode=OneWay}">
                    <tkcontrols:SettingsCard
                        Name="RobocopyUI_UseLegacySaveMode"
                        x:Uid="RobocopyUI_UseLegacySaveMode"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE7B8;}">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.UseLegacySaveMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </tkcontrols:SettingsCard>
                </controls:SettingsGroup>
            </StackPanel>
        </controls:SettingsPageControl.ModuleContent>
        <controls:SettingsPageControl.PrimaryLinks>
            <controls:PageLink x:Uid="LearnMore_RobocopyUI" Link="https://aka.ms/PowerToysOverview_RobocopyUI" />
        </controls:SettingsPageControl.PrimaryLinks>
    </controls:SettingsPageControl>
</local:NavigablePage>
