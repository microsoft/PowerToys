<local:NavigablePage
    x:Class="Microsoft.PowerToys.Settings.UI.Views.DEPiPPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <controls:SettingsPageControl
        x:Uid="DEPiP"
        IsTabStop="False"
        ModuleImageSource="ms-appx:///Assets/Settings/Modules/DEPiP.png">
        <controls:SettingsPageControl.ModuleContent>
            <StackPanel ChildrenTransitions="{StaticResource SettingsCardsAnimations}" Orientation="Vertical">
                <tkcontrols:SettingsCard
                    Name="DEPiPEnableToggleControlHeaderText"
                    x:Uid="DEPiP_EnableToggleControl_HeaderText"
                    HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/DEPiP.png}">
                    <ToggleSwitch x:Uid="ToggleSwitch" IsOn="{x:Bind ViewModel.IsEnabled, Mode=TwoWay}" />
                </tkcontrols:SettingsCard>
                <tkcontrols:SettingsCard
                    Name="DEPiPLaunchButtonControl"
                    x:Uid="DEPiP_LaunchButtonControl"
                    ActionIcon="{ui:FontIcon Glyph=&#xE8A7;}"
                    Command="{x:Bind ViewModel.LaunchEventHandler}"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE7E8;}"
                    IsClickEnabled="True"
                    IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}" />
                <InfoBar
                    x:Uid="DEPiP_DisplaySelectorInfo"
                    IsClosable="False"
                    IsOpen="True"
                    Severity="Informational" />
                <controls:SettingsGroup x:Uid="DEPiP_Settings_GroupSettings" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                    <tkcontrols:SettingsCard
                        Name="DEPiPInactiveTransparency"
                        x:Uid="DEPiP_InactiveTransparency"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE793;}">
                        <Slider
                            MinWidth="{StaticResource SettingActionControlMinWidth}"
                            LargeChange="10"
                            Maximum="90"
                            Minimum="0"
                            SmallChange="1"
                            StepFrequency="1"
                            Value="{x:Bind ViewModel.InactiveTransparency, Mode=TwoWay}" />
                    </tkcontrols:SettingsCard>
                    <tkcontrols:SettingsCard
                        Name="DEPiPLockAspectRatio"
                        x:Uid="DEPiP_LockAspectRatio"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE8A6;}">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.LockAspectRatio, Mode=TwoWay}" />
                    </tkcontrols:SettingsCard>
                    <tkcontrols:SettingsCard
                        Name="DEPiPAlwaysOnTop"
                        x:Uid="DEPiP_AlwaysOnTop"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE718;}">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.AlwaysOnTop, Mode=TwoWay}" />
                    </tkcontrols:SettingsCard>
                </controls:SettingsGroup>
            </StackPanel>
        </controls:SettingsPageControl.ModuleContent>
    </controls:SettingsPageControl>
</local:NavigablePage>
