<Page
    x:Class="Microsoft.PowerToys.Settings.UI.Views.PeekPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tk8controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ptcontrols="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <ptcontrols:SettingsPageControl x:Uid="Peek" ModuleImageSource="ms-appx:///Assets/Settings/Modules/Peek.png">
        <ptcontrols:SettingsPageControl.ModuleContent>
            <StackPanel Orientation="Vertical">
                <tk8controls:SettingsCard
                    x:Uid="Peek_EnablePeek"
                    HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/FluentIcons/FluentIconsPeek.png}"
                    IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabledGpoConfigured, Converter={StaticResource BoolNegationConverter}}">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsEnabled, Mode=TwoWay}" />
                </tk8controls:SettingsCard>
                <InfoBar
                    x:Uid="GPO_IsSettingForced"
                    IsClosable="False"
                    IsOpen="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabledGpoConfigured}"
                    IsTabStop="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabledGpoConfigured}"
                    Severity="Informational" />

                <ptcontrols:SettingsGroup x:Uid="Peek_Activation_GroupSettings" IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabled}">
                    <tk8controls:SettingsCard x:Uid="Activation_Shortcut" HeaderIcon="{ui:FontIcon Glyph=&#xEDA7;}">
                        <ptcontrols:ShortcutControl MinWidth="{StaticResource SettingActionControlMinWidth}" HotkeySettings="{x:Bind Path=ViewModel.ActivationShortcut, Mode=TwoWay}" />
                    </tk8controls:SettingsCard>
                </ptcontrols:SettingsGroup>
                <ptcontrols:SettingsGroup x:Uid="Peek_BehaviorHeader" IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabled}">
                    <tk8controls:SettingsCard x:Uid="Peek_AlwaysRunNotElevated" HeaderIcon="{ui:FontIcon Glyph=&#xE7EF;}">
                        <ToggleSwitch x:Uid="ToggleSwitch" IsOn="{x:Bind Mode=TwoWay, Path=ViewModel.AlwaysRunNotElevated}" />
                    </tk8controls:SettingsCard>
                    <tk8controls:SettingsCard x:Uid="Peek_CloseAfterLosingFocus">
                        <ToggleSwitch x:Uid="ToggleSwitch" IsOn="{x:Bind Mode=TwoWay, Path=ViewModel.CloseAfterLosingFocus}" />
                    </tk8controls:SettingsCard>
                </ptcontrols:SettingsGroup>

                <ptcontrols:SettingsGroup x:Uid="Peek_Preview_GroupSettings" IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabled}">
                    <tk8controls:SettingsExpander
                        x:Uid="Peek_SourceCode_Header"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE99A;}"
                        IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabled}">
                        <tk8controls:SettingsExpander.Items>
                            <tk8controls:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                                <CheckBox x:Uid="Peek_SourceCode_WrapText" IsChecked="{x:Bind ViewModel.SourceCodeWrapText, Mode=TwoWay}" />
                            </tk8controls:SettingsCard>
                            <tk8controls:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                                <ptcontrols:CheckBoxWithDescriptionControl
                                    x:Uid="Peek_SourceCode_TryFormat"
                                    IsChecked="{x:Bind ViewModel.SourceCodeTryFormat, Mode=TwoWay}"
                                    IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}" />
                            </tk8controls:SettingsCard>
                        </tk8controls:SettingsExpander.Items>
                    </tk8controls:SettingsExpander>
                </ptcontrols:SettingsGroup>

            </StackPanel>
        </ptcontrols:SettingsPageControl.ModuleContent>
        <ptcontrols:SettingsPageControl.PrimaryLinks>
            <ptcontrols:PageLink x:Uid="LearnMore_Peek" Link="https://aka.ms/PowerToysOverview_Peek" />
        </ptcontrols:SettingsPageControl.PrimaryLinks>
    </ptcontrols:SettingsPageControl>
</Page>
