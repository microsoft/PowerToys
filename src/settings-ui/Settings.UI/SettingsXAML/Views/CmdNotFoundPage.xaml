<Page
    x:Class="Microsoft.PowerToys.Settings.UI.Views.CmdNotFoundPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:custom="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI.UI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <custom:SettingsPageControl x:Uid="CmdNotFound" ModuleImageSource="ms-appx:///Assets/Settings/Modules/CmdNotFound.png">
        <custom:SettingsPageControl.ModuleContent>
            <StackPanel ChildrenTransitions="{StaticResource SettingsCardsAnimations}" Orientation="Vertical">
                <InfoBar
                    x:Uid="GPO_IsSettingForced"
                    IsClosable="False"
                    IsOpen="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabledGpoConfigured}"
                    IsTabStop="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabledGpoConfigured}"
                    Severity="Informational" />
                <InfoBar
                    x:Uid="CmdNotFound_ModuleExplanation"
                    IsClosable="True"
                    IsOpen="True"
                    IsTabStop="True"
                    Severity="Informational" />
                <custom:SettingsGroup x:Uid="CmdNotFound_ModuleInstallation" IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabled}">
                    <controls:SettingsCard
                        x:Uid="CmdNotFound_CheckPowerShellVersionButtonControl"
                        ActionIcon="{ui:FontIcon Glyph=&#xE8A7;}"
                        Command="{x:Bind ViewModel.CheckPowershellVersionEventHandler}"
                        IsClickEnabled="True" />
                    <controls:SettingsCard
                        x:Uid="CmdNotFound_InstallModuleButtonControl"
                        ActionIcon="{ui:FontIcon Glyph=&#xE8A7;}"
                        Command="{x:Bind ViewModel.InstallModuleEventHandler}"
                        IsClickEnabled="True" />
                    <controls:SettingsCard
                        x:Uid="CmdNotFound_UninstallModuleButtonControl"
                        ActionIcon="{ui:FontIcon Glyph=&#xE8A7;}"
                        Command="{x:Bind ViewModel.UninstallModuleEventHandler}"
                        IsClickEnabled="True" />
                </custom:SettingsGroup>
                <custom:SettingsGroup x:Uid="CmdNotFound_ModuleInstallationLogs" IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsEnabled}">
                    <TextBox
                        Height="300"
                        FontFamily="Courier New"
                        IsReadOnly="True"
                        Text="{x:Bind Mode=OneWay, Path=ViewModel.CommandOutputLog}"
                        TextWrapping="Wrap" />
                </custom:SettingsGroup>
            </StackPanel>
        </custom:SettingsPageControl.ModuleContent>

        <custom:SettingsPageControl.PrimaryLinks>
            <custom:PageLink x:Uid="LearnMore_CmdNotFound" Link="https://aka.ms/PowerToysOverview_CmdNotFound" />
        </custom:SettingsPageControl.PrimaryLinks>
    </custom:SettingsPageControl>
</Page>
