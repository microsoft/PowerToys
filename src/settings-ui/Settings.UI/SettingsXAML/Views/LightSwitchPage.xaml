<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Microsoft.PowerToys.Settings.UI.Views.LightSwitchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ViewModel="using:Microsoft.PowerToys.Settings.UI.ViewModels"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:converters="using:Microsoft.PowerToys.Settings.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Settings.UI.Library.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:tkconverters="using:CommunityToolkit.WinUI.Converters"
    xmlns:ui="using:CommunityToolkit.WinUI"
    d:DataContext="{d:DesignInstance Type=ViewModel:LightSwitchViewModel}"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
        <animations:ImplicitAnimationSet x:Name="ShowTransitions">
            <animations:OffsetAnimation
                EasingMode="EaseOut"
                From="0,24,0"
                To="0"
                Duration="0:0:0.4" />
            <animations:OpacityAnimation
                EasingMode="EaseOut"
                From="0"
                To="1"
                Duration="0:0:0.2" />
        </animations:ImplicitAnimationSet>
        <animations:ImplicitAnimationSet x:Name="HideTransitions">
            <animations:OffsetAnimation
                EasingMode="EaseOut"
                From="0"
                To="0,24,0"
                Duration="0:0:0.2" />
            <animations:OpacityAnimation
                EasingMode="EaseOut"
                From="1"
                To="0"
                Duration="0:0:0.1" />
        </animations:ImplicitAnimationSet>
    </Page.Resources>

  <controls:SettingsPageControl
        x:Uid="LightSwitch"
        IsTabStop="False"
        ModuleImageSource="ms-appx:///Assets/Settings/Modules/Awake.png">
    <controls:SettingsPageControl.ModuleContent>
      <StackPanel Orientation="Vertical">
        <tkcontrols:SettingsCard
                    Header="Enable Light Switch"
                    HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/Awake.png}"
                    IsEnabled="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}">
                    <ToggleSwitch IsOn="{x:Bind ViewModel.IsEnabled, Mode=TwoWay}" />
                </tkcontrols:SettingsCard>

                <InfoBar
                    x:Uid="GPO_SettingIsManaged"
                    IsClosable="False"
                    IsOpen="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay}"
                    IsTabStop="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay}"
                    Severity="Informational" />

                <controls:SettingsGroup Header="Activation" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">

          <tkcontrols:SettingsCard x:Uid="LightSwitch_ForceLightMode_Shortcut" HeaderIcon="{ui:FontIcon Glyph=&#xE706;}">
            <controls:ShortcutControl
            MinWidth="{StaticResource SettingActionControlMinWidth}"
            AllowDisable="True"
            HotkeySettings="{x:Bind Path=ViewModel.ForceLightModeActivationShortcut, Mode=TwoWay}" />
          </tkcontrols:SettingsCard>
          <tkcontrols:SettingsCard x:Uid="LightSwitch_ForceDarkMode_Shortcut" HeaderIcon="{ui:FontIcon Glyph=&#xE708;}">
            <controls:ShortcutControl
  MinWidth="{StaticResource SettingActionControlMinWidth}"
  AllowDisable="True"
  HotkeySettings="{x:Bind Path=ViewModel.ForceDarkModeActivationShortcut, Mode=TwoWay}" />
          </tkcontrols:SettingsCard>
        </controls:SettingsGroup>

        <controls:SettingsGroup x:Uid="DarkMode_BehaviorSettingsGroup" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
          <tkcontrols:SettingsExpander
                        Description="Use time-based or location-based rules to turn on dark mode"
                        Header="Schedule dark mode"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE823;}"
                        IsExpanded="True">
                        <ComboBox
                            SelectedValue="{x:Bind ViewModel.ScheduleMode, Mode=TwoWay}"
                            SelectedValuePath="Tag"
                            SelectionChanged="ScheduleMode_SelectionChanged">
                            <ComboBoxItem Content="Manual" Tag="FixedHours" />
                            <ComboBoxItem Content="Sunset to sunrise" Tag="SunsetToSunriseGeo" />
                    </tkcontrols:SettingsCard>

                    <tkcontrols:SettingsExpander
                        Description="Determine when dark mode should be turned on"
                        Header="Set schedule"
                        HeaderIcon="{ui:FontIcon Glyph=&#xE823;}"
                        IsExpanded="True">
                        <ComboBox
                            SelectedValue="{x:Bind ViewModel.ScheduleMode, Mode=TwoWay}"
                            SelectedValuePath="Tag"
                            SelectionChanged="ScheduleMode_SelectionChanged">
                            <ComboBoxItem Content="Manual" Tag="FixedHours" />
                            <ComboBoxItem Content="Sunset to sunrise" Tag="SunsetToSunriseGeo" />
                        </ComboBox>
                        <tkcontrols:SettingsExpander.Items>
                            <tkcontrols:SettingsCard Header="Turn on darkmode" Visibility="{x:Bind ViewModel.ScheduleMode, Mode=OneWay, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=FixedHours}">
                                <TimePicker ClockIdentifier="24HourClock" Time="{x:Bind ViewModel.LightTimeTimeSpan, Mode=TwoWay}" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard Header="Turn off darkmode" Visibility="{x:Bind ViewModel.ScheduleMode, Mode=OneWay, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=FixedHours}">
                                <TimePicker ClockIdentifier="24HourClock" Time="{x:Bind ViewModel.DarkTimeTimeSpan, Mode=TwoWay}" />
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard Header="Sunrise and sunset times" Visibility="{x:Bind ViewModel.ScheduleMode, Mode=OneWay, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=SunsetToSunriseGeo}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="08:00 - 17:00" />
                                    <Button Click="SyncLocationButton_Click" Content="Sync" />
                                </StackPanel>
                            </tkcontrols:SettingsCard>
                            <tkcontrols:SettingsCard Header="Apply an offset in minutes" Visibility="{x:Bind ViewModel.ScheduleMode, Mode=OneWay, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=SunsetToSunriseGeo}">
                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Slider
                                        x:Name="geoOffsetSlider"
                                        Grid.Column="0"
                                        Maximum="60"
                                        Minimum="-60"
                                        SnapsTo="StepValues"
                                        TickFrequency="15"
                                        TickPlacement="Outside"
                                        Value="{x:Bind ViewModel.Offset, Mode=TwoWay}" />
                                    <TextBlock
                                        x:Name="geoOffsetValue"
                                        Grid.Column="1"
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind ViewModel.Offset, Mode=OneWay}" />
                                </Grid>
                            </tkcontrols:SettingsCard>

                            <tkcontrols:SettingsCard Header="Apply an offset in minutes" Visibility="{x:Bind ViewModel.ScheduleMode, Mode=OneWay, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=SunsetToSunriseUser}">
                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Slider
                                        x:Name="useroffsetSlider"
                                        Grid.Column="0"
                                        Maximum="60"
                                        Minimum="-60"
                                        SnapsTo="StepValues"
                                        TickFrequency="15"
                                        TickPlacement="Outside"
                                        Value="{x:Bind ViewModel.Offset, Mode=TwoWay}" />
                                    <TextBlock
                                        x:Name="userOffsetValue"
                                        Grid.Column="1"
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind ViewModel.Offset, Mode=OneWay}" />
                                </Grid>
                            </tkcontrols:SettingsCard>
                        </tkcontrols:SettingsExpander.Items>
                    </tkcontrols:SettingsExpander>

                    <controls:SettingsGroup Header="Behavior" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                        <tkcontrols:SettingsExpander
                            Description="Pick which parts of your PC should follow dark mode"
                            Header="Apply dark mode to"
                            HeaderIcon="{ui:FontIcon Glyph=&#xE790;}"
                            IsExpanded="True">
                            <tkcontrols:SettingsExpander.Items>
                                <tkcontrols:SettingsCard HorizontalContentAlignment="Stretch" ContentAlignment="Left">
                                    <controls:CheckBoxWithDescriptionControl
                                        Description="Taskbar, Start, and other system UI"
                                        Header="System"
                                        IsChecked="{x:Bind ViewModel.ChangeSystem, Mode=TwoWay}" />
                                </tkcontrols:SettingsCard>
                                <tkcontrols:SettingsCard HorizontalContentAlignment="Stretch" ContentAlignment="Left">
                                    <controls:CheckBoxWithDescriptionControl
                                        Description="Supported applications"
                                        Header="Apps"
                                        IsChecked="{x:Bind ViewModel.ChangeApps, Mode=TwoWay}" />
                                </tkcontrols:SettingsCard>
                            </tkcontrols:SettingsExpander.Items>
                        </tkcontrols:SettingsExpander>
                    </controls:SettingsGroup>
                    <!--  Force mode buttons  -->
                    <!--<tkcontrols:SettingsCard
        Header="Force mode now"
        HeaderIcon="{ui:FontIcon Glyph=&#xE706;}"
        Description="Apply light or dark mode immediately">
            <StackPanel Orientation="Horizontal" Spacing="12">
              <Button
        Content="Force Light"
        Command="{x:Bind ViewModel.ForceLightCommand}" />
              <Button
        Content="Force Dark"
        Command="{x:Bind ViewModel.ForceDarkCommand}" />
            </StackPanel>
          </tkcontrols:SettingsCard>-->

                    <ContentDialog
                        x:Name="LocationDialog"
                        Title="Select a location"
                        IsPrimaryButtonEnabled="True"
                        IsSecondaryButtonEnabled="True"
                        PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
                        PrimaryButtonText="Select"
                        SecondaryButtonText="Cancel">
                        <Grid MinHeight="268">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="To calculate the sunrise and sunset, enter a location to be used." />
                            <tkcontrols:Segmented
                                x:Name="locationSegmented"
                                Grid.Row="1"
                                Margin="0,16,0,12"
                                HorizontalAlignment="Stretch"
                                SelectedIndex="0">
                                <tkcontrols:SegmentedItem Content="Find my location" Tag="Auto" />
                                <tkcontrols:SegmentedItem Content="Select a city" Tag="Selection" />
                            </tkcontrols:Segmented>
                            <tkcontrols:SwitchPresenter Grid.Row="2" Value="{Binding SelectedItem.Tag, ElementName=locationSegmented}">
                                <tkcontrols:Case Value="Auto">
                                    <Grid animations:Implicit.HideAnimations="{StaticResource HideTransitions}" animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid>
                                            <TextBlock x:Name="SunTimes" VerticalAlignment="Center" />
                                            <StackPanel
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Orientation="Horizontal"
                                                Spacing="8">
                                                <ProgressRing
                                                    x:Name="SyncLoader"
                                                    Width="16"
                                                    Height="16"
                                                    IsActive="False"
                                                    Visibility="Collapsed" />
                                                <Button
                                                    x:Name="SyncButton"
                                                    HorizontalAlignment="Right"
                                                    Click="GetLocation_Click"
                                                    Content="Sync" />
                                            </StackPanel>
                                        </Grid>
                                        <TextBlock Grid.Row="1">
                                            <Run Text="Sunrise:" />
                                            <Run Text="{x:Bind ViewModel.LightTime}" />
                                        </TextBlock>
                                        <TextBlock Grid.Row="2">
                                            <Run Text="Sunset:" />
                                            <Run Text="{x:Bind ViewModel.DarkTime}" />
                                        </TextBlock>
                                    </Grid>
                                </tkcontrols:Case>
                                <tkcontrols:Case Value="Selection">
                                    <Grid animations:Implicit.HideAnimations="{StaticResource HideTransitions}" animations:Implicit.ShowAnimations="{StaticResource ShowTransitions}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <ComboBox
                                            x:Name="CityComboBox"
                                            Margin="0,0,0,8"
                                            HorizontalAlignment="Stretch"
                                            DisplayMemberPath="Display"
                                            Header="Search for a city"
                                            ItemsSource="{x:Bind ViewModel.Cities, Mode=OneWay}"
                                            PlaceholderText="Select city"
                                            SelectedItem="{x:Bind ViewModel.SelectedCity, Mode=TwoWay}">
                                            <!--<ComboBox.ItemTemplate>
                                                <DataTemplate x:DataType="helpers:City">
                                                    <StackPanel Orientation="Vertical">
                                                        <TextBlock Text="{x:Bind Name}" />
                                                        <TextBlock
                                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                            Style="{StaticResource CaptionTextBlockStyle}"
                                                            Text="{x:Bind Country}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>-->
                                        </ComboBox>
                                        <!--  Todo: Updated times from changing city  -->



                                        <TextBlock Grid.Row="1">
                                            <Run Text="Sunrise:" />
                                            <Run Text="{x:Bind ViewModel.CityTimesText, Mode=OneWay}" />
                                        </TextBlock>
                                        <TextBlock Grid.Row="2">
                                            <Run Text="Sunset:" />
                                            <Run Text="{x:Bind ViewModel.CityTimesText, Mode=OneWay}" />
                                        </TextBlock>
                                        <!--  Todo: Option to add an offset  -->
                                    </Grid>
                                </tkcontrols:Case>
                            </tkcontrols:SwitchPresenter>
                        </Grid>
                    </ContentDialog>
                </controls:SettingsGroup>
            </StackPanel>
        </controls:SettingsPageControl.ModuleContent>

    <controls:SettingsPageControl.PrimaryLinks>
      <controls:PageLink x:Uid="LearnMore_DarkMode" Link="https://aka.ms/PowerToysOverview_ZoomIt" />
    </controls:SettingsPageControl.PrimaryLinks>
  </controls:SettingsPageControl>
</Page>
