<local:NavigablePage
    x:Class="Microsoft.PowerToys.Settings.UI.Views.CmdPalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">
    <!-- x:Uid="CmdPal" ModuleImageSource="ms-appx:///Assets/Settings/Modules/CmdPal.png" CmdPal_Activation_GroupSettings     <controls:PageLink x:Uid="LearnMore_CmdPal" Link="https://aka.ms/PowerToysOverview_CmdPal" />-->
    <Grid Padding="0,32,0,0">
        <Grid
            MaxWidth="1000"
            Padding="16,16,16,16"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource OverlayCornerRadius}"
            RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <!--<Image
                Width="32"
                HorizontalAlignment="Center"
                Source="/Assets/Settings/Icons/CmdPal.png" />-->
            <TextBlock
                Grid.Row="1"
                HorizontalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Command Palette"
                TextWrapping="Wrap" />
            <TextBlock
                Grid.Row="2"
                HorizontalAlignment="Center"
                TextAlignment="Center"
                TextWrapping="Wrap">
                <Run Text="Find files, launch apps, and do a billion other things with the most extensible quick launcher." />
                <Hyperlink>
                    <Run Text="Learn more" />
                </Hyperlink>
            </TextBlock>
            <tkcontrols:OpacityMaskView
                Grid.Row="3"
                Margin="-16,16,-16,0"
                HorizontalAlignment="Stretch">
                <tkcontrols:OpacityMaskView.OpacityMask>
                    <Rectangle>
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                <GradientStop Offset="0.5" Color="White" />
                                <GradientStop Offset="1" Color="Transparent" />
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </tkcontrols:OpacityMaskView.OpacityMask>
                <Image
                    Height="360"
                    HorizontalAlignment="Stretch"
                    Source="/Assets/Settings/Modules/CmdPal.png"
                    Stretch="UniformToFill" />
            </tkcontrols:OpacityMaskView>

            <Grid
                Grid.Row="4"
                Margin="0,12,0,48"
                ColumnSpacing="32"
                RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <FontIcon
                    HorizontalAlignment="Center"
                    FontSize="28"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xEB3B;" />
                <TextBlock
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    TextAlignment="Center"
                    TextWrapping="Wrap">
                    <Run FontWeight="SemiBold" Text="Extensible" /> <LineBreak />
                    <Run Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Powerful extensions help you do more" />
                </TextBlock>

                <FontIcon
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    FontSize="28"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE945;" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    TextAlignment="Center"
                    TextWrapping="Wrap">
                    <Run FontWeight="SemiBold" Text="Fast" /> <LineBreak />
                    <Run Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Find files and launch apps in an instant" />
                </TextBlock>

                <FontIcon
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    FontSize="28"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE790;" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="2"
                    HorizontalAlignment="Center"
                    TextAlignment="Center"
                    TextWrapping="Wrap">
                    <Run FontWeight="SemiBold" Text="Beautiful" /> <LineBreak />
                    <Run Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="A modern experience that feels right at home on Windows" />
                </TextBlock>
            </Grid>


            <tkcontrols:SettingsCard
                Name="CmdPalEnableCmdPal"
                x:Uid="CmdPal_Enable_CmdPal"
                Grid.Row="5"
                HorizontalAlignment="Stretch"
                HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/CmdPal.png}">
                <ToggleSwitch IsOn="{x:Bind ViewModel.IsEnabled, Mode=TwoWay}" />
            </tkcontrols:SettingsCard>
            <tkcontrols:SettingsCard
                Grid.Row="6"
                Header="Open Command Palette"
                IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                <tkcontrols:SettingsCard.Description>
                    <HyperlinkButton
                        x:Name="CmdPalSettingsDeeplink"
                        Click="CmdPalSettingsDeeplink_Click"
                        Content="Configure Command Palette settings"
                        IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}" />
                </tkcontrols:SettingsCard.Description>
                <StackPanel
                    HorizontalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button Content="Launch" Click="LaunchButton_Click" Style="{StaticResource AccentButtonStyle}" />
                    <controls:ShortcutWithTextLabelControl
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Keys="{x:Bind Path=ViewModel.Hotkey.GetKeysList(), Mode=OneWay}"
                        LabelPlacement="Before"
                        Tapped="ShortcutWithTextLabelControl_Tapped"
                        Text="or" />
                </StackPanel>
            </tkcontrols:SettingsCard>
        </Grid>


        <!--<controls:GPOInfoControl ShowWarning="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay}">
            <tkcontrols:SettingsCard
                Name="CmdPalEnableCmdPal"
                x:Uid="CmdPal_Enable_CmdPal"
                HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/CmdPal.png}" />
        </controls:GPOInfoControl>-->


        <!--<tkcontrols:SettingsCard
            Name="CmdPalActivationShortcut"
            x:Uid="CmdPal_ActivationShortcut"
            HeaderIcon="{ui:FontIcon Glyph=&#xEDA7;}">
            <controls:ShortcutControl
                MinWidth="{StaticResource SettingActionControlMinWidth}"
                HotkeySettings="{x:Bind Path=ViewModel.Hotkey, Mode=OneWay}"
                IsEnabled="False" />
            <tkcontrols:SettingsCard.Description />
        </tkcontrols:SettingsCard>-->
    </Grid>
</local:NavigablePage>
