<local:NavigablePage
    x:Class="Microsoft.PowerToys.Settings.UI.Views.CopyAsUNCPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <controls:SettingsPageControl x:Uid="CopyAsUNC" ModuleImageSource="ms-appx:///Assets/Settings/Modules/CopyAsUNC.png">
        <controls:SettingsPageControl.ModuleContent>
            <StackPanel ChildrenTransitions="{StaticResource SettingsCardsAnimations}" Orientation="Vertical">
                <controls:GPOInfoControl ShowWarning="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay}">
                    <tkcontrols:SettingsCard
                        Name="CopyAsUNCEnable"
                        x:Uid="CopyAsUNC_Enable"
                        HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/CopyAsUNC.png}">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.IsCopyAsUNCEnabled, Mode=TwoWay}" />
                    </tkcontrols:SettingsCard>
                </controls:GPOInfoControl>

                <controls:SettingsGroup x:Uid="CopyAsUNC_ShellIntegration" IsEnabled="{x:Bind ViewModel.IsCopyAsUNCEnabled, Mode=OneWay}">
                    <tkcontrols:SettingsCard Name="CopyAsUNCToggleContextMenu" x:Uid="CopyAsUNC_Toggle_ContextMenu">
                        <ComboBox MinWidth="{StaticResource SettingActionControlMinWidth}" SelectedIndex="{x:Bind ViewModel.EnabledOnContextExtendedMenu, Mode=TwoWay, Converter={StaticResource BoolToComboBoxIndexConverter}}">
                            <ComboBoxItem x:Uid="CopyAsUNC_Toggle_StandardContextMenu" />
                            <ComboBoxItem x:Uid="CopyAsUNC_Toggle_ExtendedContextMenu" />
                        </ComboBox>
                    </tkcontrols:SettingsCard>
                    <InfoBar
                        x:Uid="ExtendedContextMenuInfo"
                        IsClosable="False"
                        IsOpen="True"
                        IsTabStop="True"
                        Severity="Informational" />
                </controls:SettingsGroup>
            </StackPanel>
        </controls:SettingsPageControl.ModuleContent>
        <controls:SettingsPageControl.PrimaryLinks>
            <controls:PageLink x:Uid="LearnMore_CopyAsUNC" Link="https://aka.ms/PowerToysOverview_CopyAsUNC" />
        </controls:SettingsPageControl.PrimaryLinks>
    </controls:SettingsPageControl>
</local:NavigablePage>
