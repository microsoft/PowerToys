<ContentDialog
    x:Class="Microsoft.PowerToys.Settings.UI.Views.MonitorBlacklistDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">
    <!--  DIAGNOSTIC: stripped to bare minimum to bisect the persistent XamlParseException
         (HResult 0x80073B0A = ERROR_MRM_INVALID_QUALIFIER). If this minimal dialog opens
         OK, the bug is in the previous content (likely an x:Uid / resw key); if it still
         crashes, the bug is in the dialog wrapper / page integration.  -->
    <TextBlock Text="Diagnostic: dialog opens." />
</ContentDialog>
