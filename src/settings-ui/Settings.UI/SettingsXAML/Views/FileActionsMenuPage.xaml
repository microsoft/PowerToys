<Page
    x:Class="Microsoft.PowerToys.Settings.UI.Views.FileActionsMenuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <controls:SettingsPageControl x:Uid="FileActionsMenu" ModuleImageSource="ms-appx:///Assets/Settings/Modules/FileActionsMenu.png">
        <controls:SettingsPageControl.ModuleContent>
            <StackPanel ChildrenTransitions="{StaticResource SettingsCardsAnimations}" Orientation="Vertical">
                <tkcontrols:SettingsCard
                    x:Uid="FileActionsMenu_ToggleSwitch_FileActionsMenu"
                    HeaderIcon="{ui:FontIcon Glyph=&#xE943;}"
                    IsEnabled="{x:Bind Mode=OneWay, Path=ViewModel.IsFileActionsMenuEnabledGpoConfigured, Converter={StaticResource BoolNegationConverter}}">
                    <ToggleSwitch x:Uid="ToggleSwitch" IsOn="{x:Bind Mode=TwoWay, Path=ViewModel.FileActionsMenuIsEnabled}" />
                </tkcontrols:SettingsCard>
                <tkcontrols:SettingsCard
                    x:Uid="Activation_Shortcut"
                    HeaderIcon="{ui:FontIcon Glyph=&#xEDA7;}"
                    IsEnabled="{x:Bind ViewModel.FileActionsMenuIsEnabled, Mode=OneWay}">
                    <controls:ShortcutControl MinWidth="{StaticResource SettingActionControlMinWidth}" HotkeySettings="{x:Bind Path=ViewModel.FileActionsMenuShortcut, Mode=TwoWay}" />
                </tkcontrols:SettingsCard>

            </StackPanel>
        </controls:SettingsPageControl.ModuleContent>

        <controls:SettingsPageControl.PrimaryLinks>
            <controls:PageLink x:Uid="LearnMore_FileActionsMenu" Link="https://aka.ms/PowerToysOverview_FileActionsMenu" />
        </controls:SettingsPageControl.PrimaryLinks>
        <controls:SettingsPageControl.SecondaryLinks>
            <controls:PageLink Link="https://blog.aaron-junker.ch" Text="Aaron Junker's work on File Actions Menu" />
        </controls:SettingsPageControl.SecondaryLinks>
    </controls:SettingsPageControl>
</Page>
