<ContentDialog
    x:Class="Microsoft.PowerToys.Settings.UI.Views.DangerousFeatureWarningDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DefaultButton="Close"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <StackPanel Spacing="12">
        <TextBlock
            x:Name="WarningHeader"
            FontWeight="Bold"
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            TextWrapping="Wrap" />
        <TextBlock x:Name="WarningDescription" TextWrapping="Wrap" />
        <StackPanel Margin="20,0,0,0" Spacing="4">
            <TextBlock x:Name="WarningListItem1" TextWrapping="Wrap" />
            <TextBlock x:Name="WarningListItem2" TextWrapping="Wrap" />
            <TextBlock x:Name="WarningListItem3" TextWrapping="Wrap" />
        </StackPanel>
        <TextBlock
            x:Name="WarningConfirm"
            FontWeight="SemiBold"
            TextWrapping="Wrap" />
    </StackPanel>
</ContentDialog>
