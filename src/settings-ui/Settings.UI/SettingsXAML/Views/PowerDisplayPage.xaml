<local:NavigablePage
    x:Class="Microsoft.PowerToys.Settings.UI.Views.PowerDisplayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:library="using:Microsoft.PowerToys.Settings.UI.Library"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pdmodels="using:PowerDisplay.Models"
    xmlns:ptcontrols="using:Microsoft.PowerToys.Common.UI.Controls"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    AutomationProperties.LandmarkType="Main"
    mc:Ignorable="d">

    <local:NavigablePage.Resources>
        <x:Double x:Key="PowerDisplayCompactActionControlMinWidth">120</x:Double>
        <x:Double x:Key="PowerDisplayVcpDetailsFlyoutWidth">420</x:Double>
        <x:Double x:Key="PowerDisplayVcpDetailsFlyoutMaxHeight">480</x:Double>
        <x:Double x:Key="PowerDisplayActionButtonSpacing">8</x:Double>
        <x:Double x:Key="PowerDisplayCompactSpacing">4</x:Double>
        <x:Double x:Key="PowerDisplayInlineIconSpacing">6</x:Double>
        <x:Double x:Key="PowerDisplayPrimaryGlyphFontSize">14</x:Double>
        <x:Double x:Key="PowerDisplaySecondaryGlyphFontSize">12</x:Double>
        <x:Double x:Key="PowerDisplayMoreButtonGlyphFontSize">16</x:Double>
        <Thickness x:Key="PowerDisplayMonitorExpanderMargin">0,0,0,2</Thickness>
        <Thickness x:Key="PowerDisplayVcpDetailsHeaderMargin">0,0,0,12</Thickness>
        <Thickness x:Key="PowerDisplayVcpCopyButtonPadding">8,4</Thickness>
        <Thickness x:Key="PowerDisplayVcpValueMargin">0,0,0,8</Thickness>
    </local:NavigablePage.Resources>

    <controls:SettingsPageControl x:Uid="PowerDisplay" ModuleImageSource="ms-appx:///Assets/Settings/Modules/PowerDisplay.png">
        <controls:SettingsPageControl.ModuleContent>
            <!--  ChildrenTransitions intentionally omitted: RepositionThemeTransition interacts badly  -->
            <!--  with the per-monitor SettingsExpanders and PageScrollViewer, causing rubber-band  -->
            <!--  scrolling when a monitor is expanded.  -->
            <StackPanel>
                <!--  Crash recovery banner. Outside the inner StackPanel so the Ignore button stays clickable when the rest of the page is locked.  -->
                <InfoBar
                    x:Uid="PowerDisplay_CrashDetectedInfoBar"
                    IsClosable="False"
                    IsOpen="{x:Bind ViewModel.IsCrashLockActive, Mode=OneWay}"
                    Severity="Error">
                    <InfoBar.ActionButton>
                        <Button x:Uid="PowerDisplay_CrashDetected_IgnoreButton" Command="{x:Bind ViewModel.DismissCrashWarningCommand}" />
                    </InfoBar.ActionButton>
                </InfoBar>

                <!--  Inner wrapper: everything below is locked when crash flag is present. ContentControl carries IsEnabled because Panel/StackPanel does not derive from Control. IsTabStop/AccessibilityView keep it invisible to tab navigation and Narrator (purely structural).  -->
                <ContentControl
                    HorizontalContentAlignment="Stretch"
                    AutomationProperties.AccessibilityView="Raw"
                    IsEnabled="{x:Bind ViewModel.IsCrashLockActive, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
                    IsTabStop="False">
                    <StackPanel>
                        <controls:GPOInfoControl ShowWarning="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay}">
                            <tkcontrols:SettingsCard
                                x:Uid="PowerDisplay_Enable_PowerDisplay"
                                HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/PowerDisplay.png}"
                                IsEnabled="{x:Bind ViewModel.IsEnabledGpoConfigured, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}">
                                <ToggleSwitch IsOn="{x:Bind ViewModel.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </tkcontrols:SettingsCard>
                        </controls:GPOInfoControl>
                        <controls:SettingsGroup x:Uid="PowerDisplayGroupHeader" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                            <tkcontrols:SettingsExpander
                                x:Uid="PowerDisplayFlyoutHeader"
                                HeaderIcon="{ui:FontIcon Glyph=&#xE770;}"
                                IsExpanded="True">
                                <Button
                                    x:Uid="PowerDisplay_LaunchButton"
                                    Command="{x:Bind ViewModel.LaunchEventHandler}"
                                    Style="{StaticResource AccentButtonStyle}" />
                                <tkcontrols:SettingsExpander.Items>
                                    <!--  HACK: For some weird reason, a ShortcutControl does not work correctly if it's the first or last item in the expander, so we add an invisible card.  -->
                                    <tkcontrols:SettingsCard Visibility="Collapsed" />
                                    <tkcontrols:SettingsCard x:Uid="PowerDisplay_ActivationShortcut">
                                        <controls:ShortcutControl HotkeySettings="{x:Bind Path=ViewModel.ActivationShortcut, Mode=TwoWay}" />
                                    </tkcontrols:SettingsCard>

                                    <tkcontrols:SettingsCard x:Uid="PowerDisplay_MonitorRefreshDelay">
                                        <ComboBox
                                            MinWidth="{StaticResource PowerDisplayCompactActionControlMinWidth}"
                                            ItemsSource="{x:Bind ViewModel.MonitorRefreshDelayOptions}"
                                            SelectedItem="{x:Bind ViewModel.MonitorRefreshDelay, Mode=TwoWay}" />
                                    </tkcontrols:SettingsCard>
                                    <tkcontrols:SettingsCard x:Uid="PowerDisplay_MouseWheelControlMode">
                                        <ComboBox
                                            x:Uid="PowerDisplay_MouseWheelControlModeComboBox"
                                            MinWidth="{StaticResource PowerDisplayCompactActionControlMinWidth}"
                                            SelectedIndex="{x:Bind ViewModel.MouseWheelControlModeIndex, Mode=TwoWay}">
                                            <ComboBoxItem x:Uid="PowerDisplay_MouseWheelControlMode_Off" />
                                            <ComboBoxItem x:Uid="PowerDisplay_MouseWheelControlMode_PrimaryDisplay" />
                                            <ComboBoxItem x:Uid="PowerDisplay_MouseWheelControlMode_AllDisplays" />
                                        </ComboBox>
                                    </tkcontrols:SettingsCard>
                                    <!--
                                        Deliberately not gated on the tray icon wheel mode: this step
                                        is shared by the flyout sliders and by the command-line
                                        relative adjust (AdjustCommandExecutor), both of which stay
                                        available when tray scrolling is off.
                                    -->
                                    <tkcontrols:SettingsCard x:Uid="PowerDisplay_MouseWheelIncrement">
                                        <ComboBox
                                            MinWidth="{StaticResource PowerDisplayCompactActionControlMinWidth}"
                                            ItemsSource="{x:Bind ViewModel.MouseWheelIncrementOptions}"
                                            SelectedItem="{x:Bind ViewModel.MouseWheelIncrement, Mode=TwoWay}" />
                                    </tkcontrols:SettingsCard>
                                    <tkcontrols:SettingsCard ContentAlignment="Left">
                                        <CheckBox x:Uid="PowerDisplay_RestoreSettingsOnStartup" IsChecked="{x:Bind ViewModel.RestoreSettingsOnStartup, Mode=TwoWay}" />
                                    </tkcontrols:SettingsCard>
                                    <tkcontrols:SettingsCard ContentAlignment="Left">
                                        <CheckBox x:Uid="PowerDisplay_ShowSystemTrayIcon" IsChecked="{x:Bind ViewModel.ShowSystemTrayIcon, Mode=TwoWay}" />
                                    </tkcontrols:SettingsCard>
                                    <tkcontrols:SettingsCard ContentAlignment="Left">
                                        <CheckBox x:Uid="PowerDisplay_ShowProfileSwitcher" IsChecked="{x:Bind ViewModel.ShowProfileSwitcher, Mode=TwoWay}" />
                                    </tkcontrols:SettingsCard>
                                    <tkcontrols:SettingsCard ContentAlignment="Left">
                                        <CheckBox x:Uid="PowerDisplay_ShowIdentifyMonitorsButton" IsChecked="{x:Bind ViewModel.ShowIdentifyMonitorsButton, Mode=TwoWay}" />
                                    </tkcontrols:SettingsCard>
                                </tkcontrols:SettingsExpander.Items>
                            </tkcontrols:SettingsExpander>
                        </controls:SettingsGroup>

                        <controls:SettingsGroup x:Uid="PowerDisplay_AdvancedSettings" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                            <tkcontrols:SettingsCard x:Uid="PowerDisplay_MaxCompatibilityMode" HeaderIcon="{ui:FontIcon Glyph=&#xE7B5;}">
                                <ToggleSwitch IsOn="{x:Bind ViewModel.MaxCompatibilityMode, Mode=TwoWay}" />
                            </tkcontrols:SettingsCard>
                        </controls:SettingsGroup>

                        <!--  Custom VCP Name Mappings  -->
                        <controls:SettingsGroup x:Uid="PowerDisplay_CustomVcpMappings_GroupSettings" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                            <tkcontrols:SettingsExpander
                                x:Uid="PowerDisplay_CustomVcpMappings"
                                HeaderIcon="{ui:FontIcon Glyph=&#xE70F;}"
                                IsExpanded="{x:Bind ViewModel.HasCustomVcpMappings, Mode=OneWay}"
                                ItemsSource="{x:Bind ViewModel.CustomVcpMappings, Mode=OneWay}">
                                <tkcontrols:SettingsExpander.ItemTemplate>
                                    <DataTemplate x:DataType="pdmodels:CustomVcpValueMapping">
                                        <tkcontrols:SettingsCard Description="{x:Bind VcpCodeDisplayName}" Header="{x:Bind DisplaySummary}">
                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource PowerDisplayActionButtonSpacing}">
                                                <Button
                                                    x:Uid="PowerDisplay_EditCustomMapping_Button"
                                                    Click="EditCustomMapping_Click"
                                                    Content="{ui:FontIcon Glyph=&#xE70F;,
                                                                          FontSize={StaticResource PowerDisplayPrimaryGlyphFontSize}}"
                                                    Style="{StaticResource SubtleButtonStyle}"
                                                    Tag="{x:Bind}" />
                                                <Button
                                                    x:Uid="PowerDisplay_DeleteCustomMapping_Button"
                                                    Click="DeleteCustomMapping_Click"
                                                    Content="{ui:FontIcon Glyph=&#xE74D;,
                                                                          FontSize={StaticResource PowerDisplayPrimaryGlyphFontSize}}"
                                                    Style="{StaticResource SubtleButtonStyle}"
                                                    Tag="{x:Bind}" />
                                            </StackPanel>
                                        </tkcontrols:SettingsCard>
                                    </DataTemplate>
                                </tkcontrols:SettingsExpander.ItemTemplate>

                                <!--  Add mapping button  -->
                                <Button x:Uid="PowerDisplay_AddCustomMappingButton" Click="AddCustomMapping_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource PowerDisplayInlineIconSpacing}">
                                        <FontIcon FontSize="{StaticResource PowerDisplayPrimaryGlyphFontSize}" Glyph="&#xE710;" />
                                        <TextBlock x:Uid="PowerDisplay_AddCustomMapping_Text" />
                                    </StackPanel>
                                </Button>
                            </tkcontrols:SettingsExpander>
                        </controls:SettingsGroup>

                        <controls:SettingsGroup x:Uid="PowerDisplay_Profiles_GroupSettings" IsEnabled="{x:Bind ViewModel.CanUseProfiles, Mode=OneWay}">
                            <tkcontrols:SettingsExpander
                                x:Uid="PowerDisplay_QuickProfiles"
                                HeaderIcon="{ui:FontIcon Glyph=&#xE8B7;}"
                                IsExpanded="{x:Bind ViewModel.HasProfiles, Mode=OneWay}"
                                ItemContainerStyleSelector="{x:Null}">
                                <tkcontrols:SettingsExpander.Items>
                                    <ListView
                                        x:Name="ProfilesList"
                                        x:Uid="PowerDisplay_ProfileList"
                                        AllowDrop="{x:Bind ViewModel.CanDragProfiles, Mode=OneWay}"
                                        CanDragItems="{x:Bind ViewModel.CanDragProfiles, Mode=OneWay}"
                                        CanReorderItems="{x:Bind ViewModel.CanDragProfiles, Mode=OneWay}"
                                        DragItemsCompleted="ProfilesList_DragItemsCompleted"
                                        DragItemsStarting="ProfilesList_DragItemsStarting"
                                        IsTabStop="False"
                                        ItemsSource="{x:Bind ViewModel.Profiles, Mode=OneWay}"
                                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                        ScrollViewer.HorizontalScrollMode="Disabled"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                                        ScrollViewer.VerticalScrollMode="Auto"
                                        SelectionMode="None">
                                        <ListView.ItemTemplate>
                                            <DataTemplate x:DataType="pdmodels:PowerDisplayProfile">
                                                <tkcontrols:SettingsCard
                                                    Padding="16,8,44,8"
                                                    Header="{x:Bind DisplayName}"
                                                    Style="{StaticResource DefaultSettingsExpanderItemStyle}">
                                                    <tkcontrols:SettingsCard.HeaderIcon>
                                                        <PathIcon
                                                            x:Uid="PowerDisplay_Profile_DragHandle"
                                                            Height="{StaticResource PowerDisplayMoreButtonGlyphFontSize}"
                                                            AutomationProperties.AccessibilityView="Raw"
                                                            Data="M15.5 17C16.3284 17 17 17.6716 17 18.5C17 19.3284 16.3284 20 15.5 20C14.6716 20 14 19.3284 14 18.5C14 17.6716 14.6716 17 15.5 17ZM8.5 17C9.32843 17 10 17.6716 10 18.5C10 19.3284 9.32843 20 8.5 20C7.67157 20 7 19.3284 7 18.5C7 17.6716 7.67157 17 8.5 17ZM15.5 10C16.3284 10 17 10.6716 17 11.5C17 12.3284 16.3284 13 15.5 13C14.6716 13 14 12.3284 14 11.5C14 10.6716 14.6716 10 15.5 10ZM8.5 10C9.32843 10 10 10.6716 10 11.5C10 12.3284 9.32843 13 8.5 13C7.67157 13 7 12.3284 7 11.5C7 10.6716 7.67157 10 8.5 10ZM15.5 3C16.3284 3 17 3.67157 17 4.5C17 5.32843 16.3284 6 15.5 6C14.6716 6 14 5.32843 14 4.5C14 3.67157 14.6716 3 15.5 3ZM8.5 3C9.32843 3 10 3.67157 10 4.5C10 5.32843 9.32843 6 8.5 6C7.67157 6 7 5.32843 7 4.5C7 3.67157 7.67157 3 8.5 3Z"
                                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                                                    </tkcontrols:SettingsCard.HeaderIcon>
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource PowerDisplayActionButtonSpacing}">
                                                        <Button
                                                            x:Uid="PowerDisplay_Profile_ApplyButton"
                                                            AutomationProperties.HelpText="{x:Bind DisplayName}"
                                                            Click="ProfileButton_Click"
                                                            Tag="{x:Bind}" />
                                                        <Button
                                                            x:Name="ProfileMoreButton"
                                                            x:Uid="PowerDisplay_Profile_MoreButton"
                                                            AutomationProperties.HelpText="{x:Bind DisplayName}"
                                                            Content="{ui:FontIcon Glyph=&#xE712;,
                                                                                  FontSize={StaticResource PowerDisplayMoreButtonGlyphFontSize}}"
                                                            Style="{StaticResource SubtleButtonStyle}"
                                                            Tag="{x:Bind}">
                                                            <Button.Flyout>
                                                                <MenuFlyout Opening="ProfileMenuFlyout_Opening">
                                                                    <MenuFlyoutItem
                                                                        x:Uid="PowerDisplay_Profile_EditMenuItem"
                                                                        Click="EditProfile_Click"
                                                                        Icon="{ui:FontIcon Glyph=&#xE70F;}"
                                                                        Tag="{x:Bind}" />
                                                                    <MenuFlyoutSeparator />
                                                                    <MenuFlyoutItem
                                                                        x:Name="MoveProfileUpMenuItem"
                                                                        x:Uid="MoveUp"
                                                                        Click="MoveProfileUp_Click"
                                                                        Icon="{ui:FontIcon Glyph=&#xE74A;}"
                                                                        Tag="{x:Bind}" />
                                                                    <MenuFlyoutItem
                                                                        x:Name="MoveProfileDownMenuItem"
                                                                        x:Uid="MoveDown"
                                                                        Click="MoveProfileDown_Click"
                                                                        Icon="{ui:FontIcon Glyph=&#xE74B;}"
                                                                        Tag="{x:Bind}" />
                                                                    <MenuFlyoutSeparator />
                                                                    <MenuFlyoutItem
                                                                        x:Uid="PowerDisplay_Profile_DeleteMenuItem"
                                                                        Click="DeleteProfile_Click"
                                                                        Icon="{ui:FontIcon Glyph=&#xE74D;}"
                                                                        Tag="{x:Bind}" />
                                                                </MenuFlyout>
                                                            </Button.Flyout>
                                                        </Button>
                                                    </StackPanel>
                                                </tkcontrols:SettingsCard>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </tkcontrols:SettingsExpander.Items>

                                <!--  Add profile button  -->
                                <Button x:Uid="PowerDisplay_AddProfileButton" Click="AddProfileButton_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource PowerDisplayInlineIconSpacing}">
                                        <FontIcon FontSize="{StaticResource PowerDisplayPrimaryGlyphFontSize}" Glyph="&#xE710;" />
                                        <TextBlock x:Uid="PowerDisplay_AddProfile_Text" />
                                    </StackPanel>
                                </Button>
                            </tkcontrols:SettingsExpander>
                            <InfoBar
                                x:Uid="PowerDisplay_Profile_DragDisabledByElevation"
                                IsClosable="False"
                                IsOpen="{x:Bind ViewModel.IsProfileDragDisabledByElevation, Mode=OneWay}"
                                IsTabStop="{x:Bind ViewModel.IsProfileDragDisabledByElevation, Mode=OneWay}"
                                Severity="Informational" />
                            <InfoBar
                                x:Uid="PowerDisplay_Profile_ReorderError"
                                IsClosable="False"
                                IsOpen="{x:Bind ViewModel.IsProfileReorderError, Mode=OneWay}"
                                IsTabStop="{x:Bind ViewModel.IsProfileReorderError, Mode=OneWay}"
                                Severity="Error" />
                        </controls:SettingsGroup>

                        <controls:SettingsGroup x:Uid="PowerDisplay_Monitors" IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}">
                            <!--  Empty state hint  -->
                            <TextBlock
                                x:Uid="PowerDisplay_NoMonitorsDetected"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Visibility="{x:Bind ViewModel.HasMonitors, Mode=OneWay, Converter={StaticResource ReverseBoolToVisibilityConverter}}" />

                            <!--  Monitor list  -->
                            <ItemsControl
                                x:Name="MonitorsList"
                                ItemsSource="{x:Bind ViewModel.Monitors, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.HasMonitors, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="library:MonitorInfo">
                                        <tkcontrols:SettingsExpander
                                            Margin="{StaticResource PowerDisplayMonitorExpanderMargin}"
                                            Description="{x:Bind Id, Mode=OneWay}"
                                            Header="{x:Bind DisplayName, Mode=OneWay}">
                                            <tkcontrols:SettingsExpander.HeaderIcon>
                                                <FontIcon Glyph="{x:Bind MonitorIconGlyph, Mode=OneWay}" />
                                            </tkcontrols:SettingsExpander.HeaderIcon>
                                            <ptcontrols:IsEnabledTextBlock Text="{x:Bind CommunicationMethod, Mode=OneWay}" />
                                            <tkcontrols:SettingsExpander.ItemsHeader>
                                                <!--  Capabilities warning  -->
                                                <InfoBar
                                                    x:Uid="PowerDisplay_Monitor_CapabilitiesWarning"
                                                    BorderThickness="0"
                                                    CornerRadius="0"
                                                    IsClosable="False"
                                                    IsOpen="{x:Bind ShowCapabilitiesWarning, Mode=OneWay}"
                                                    Severity="Warning" />
                                            </tkcontrols:SettingsExpander.ItemsHeader>
                                            <tkcontrols:SettingsExpander.Items>
                                                <tkcontrols:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind SupportsContrast, Mode=OneWay}">
                                                    <CheckBox x:Uid="PowerDisplay_Monitor_EnableContrast" IsChecked="{x:Bind EnableContrast, Mode=TwoWay}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind SupportsVolume, Mode=OneWay}">
                                                    <CheckBox x:Uid="PowerDisplay_Monitor_EnableVolume" IsChecked="{x:Bind EnableVolume, Mode=TwoWay}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind SupportsInputSource, Mode=OneWay}">
                                                    <CheckBox
                                                        x:Uid="PowerDisplay_Monitor_EnableInputSource"
                                                        Click="EnableInputSource_Click"
                                                        IsChecked="{x:Bind EnableInputSource, Mode=OneWay}"
                                                        Tag="{x:Bind}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard ContentAlignment="Left">
                                                    <CheckBox x:Uid="PowerDisplay_Monitor_EnableRotation" IsChecked="{x:Bind EnableRotation, Mode=TwoWay}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind SupportsColorTemperature, Mode=OneWay}">
                                                    <CheckBox
                                                        x:Uid="PowerDisplay_Monitor_EnableColorTemperature"
                                                        Click="EnableColorTemperature_Click"
                                                        IsChecked="{x:Bind EnableColorTemperature, Mode=OneWay}"
                                                        Tag="{x:Bind}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard ContentAlignment="Left" IsEnabled="{x:Bind SupportsPowerState, Mode=OneWay}">
                                                    <CheckBox
                                                        x:Uid="PowerDisplay_Monitor_EnablePowerState"
                                                        Click="EnablePowerState_Click"
                                                        IsChecked="{x:Bind EnablePowerState, Mode=OneWay}"
                                                        Tag="{x:Bind}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard ContentAlignment="Left">
                                                    <CheckBox x:Uid="PowerDisplay_Monitor_HideMonitor" IsChecked="{x:Bind IsHidden, Mode=TwoWay}" />
                                                </tkcontrols:SettingsCard>
                                                <tkcontrols:SettingsCard x:Uid="PowerDisplay_Monitor_Diagnostics">
                                                    <Button
                                                        x:Uid="PowerDisplay_Monitor_Diagnostics_CopyButton"
                                                        Click="CopyMonitorDiagnostics_Click"
                                                        Tag="{x:Bind}">
                                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource PowerDisplayInlineIconSpacing}">
                                                            <FontIcon FontSize="{StaticResource PowerDisplayPrimaryGlyphFontSize}" Glyph="&#xE8C8;" />
                                                            <TextBlock x:Uid="PowerDisplay_Monitor_Diagnostics_CopyText" />
                                                        </StackPanel>
                                                    </Button>
                                                </tkcontrols:SettingsCard>

                                                <!--  VCP Capabilities  -->
                                                <tkcontrols:SettingsCard x:Uid="PowerDisplay_Monitor_VcpCapabilities" Visibility="{x:Bind HasCapabilities, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                    <Button
                                                        x:Uid="PowerDisplay_Monitor_VcpDetails_Button"
                                                        Content="{ui:FontIcon Glyph=&#xE946;}"
                                                        Style="{StaticResource SubtleButtonStyle}">
                                                        <Button.Flyout>
                                                            <Flyout ShouldConstrainToRootBounds="False">
                                                                <Grid Width="{StaticResource PowerDisplayVcpDetailsFlyoutWidth}">
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition />
                                                                    </Grid.RowDefinitions>
                                                                    <!--  Header with Copy Button  -->
                                                                    <Grid Margin="{StaticResource PowerDisplayVcpDetailsHeaderMargin}">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="Auto" />
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock
                                                                            x:Uid="PowerDisplay_Monitor_VcpCodes_Header"
                                                                            VerticalAlignment="Center"
                                                                            FontSize="{StaticResource PowerDisplayPrimaryGlyphFontSize}"
                                                                            FontWeight="SemiBold" />
                                                                        <Button
                                                                            x:Uid="PowerDisplay_Monitor_VcpCodes_CopyButton"
                                                                            Grid.Column="1"
                                                                            Padding="{StaticResource PowerDisplayVcpCopyButtonPadding}"
                                                                            VerticalAlignment="Center"
                                                                            Click="CopyVcpCodes_Click"
                                                                            Tag="{x:Bind}">
                                                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource PowerDisplayCompactSpacing}">
                                                                                <FontIcon FontSize="{StaticResource PowerDisplaySecondaryGlyphFontSize}" Glyph="&#xE8C8;" />
                                                                                <TextBlock x:Uid="PowerDisplay_Monitor_VcpCodes_CopyText" FontSize="{StaticResource PowerDisplaySecondaryGlyphFontSize}" />
                                                                            </StackPanel>
                                                                        </Button>
                                                                    </Grid>

                                                                    <!--  VCP Codes List  -->
                                                                    <ScrollViewer
                                                                        Grid.Row="1"
                                                                        MaxHeight="{StaticResource PowerDisplayVcpDetailsFlyoutMaxHeight}"
                                                                        HorizontalScrollBarVisibility="Disabled"
                                                                        HorizontalScrollMode="Disabled"
                                                                        VerticalScrollBarVisibility="Auto">
                                                                        <ItemsControl ItemsSource="{x:Bind VcpCodesFormatted, Mode=OneWay}">
                                                                            <ItemsControl.ItemTemplate>
                                                                                <DataTemplate x:DataType="library:VcpCodeDisplayInfo">
                                                                                    <StackPanel>
                                                                                        <TextBlock
                                                                                            FontSize="{StaticResource PowerDisplaySecondaryGlyphFontSize}"
                                                                                            Text="{x:Bind Title}"
                                                                                            TextWrapping="Wrap" />
                                                                                        <TextBlock
                                                                                            Margin="{StaticResource PowerDisplayVcpValueMargin}"
                                                                                            FontSize="{StaticResource PowerDisplaySecondaryGlyphFontSize}"
                                                                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                                            Text="{x:Bind Values}"
                                                                                            TextWrapping="Wrap"
                                                                                            Visibility="{x:Bind HasValues, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                                                    </StackPanel>
                                                                                </DataTemplate>
                                                                            </ItemsControl.ItemTemplate>
                                                                        </ItemsControl>
                                                                    </ScrollViewer>
                                                                </Grid>
                                                            </Flyout>
                                                        </Button.Flyout>
                                                    </Button>
                                                </tkcontrols:SettingsCard>
                                            </tkcontrols:SettingsExpander.Items>
                                        </tkcontrols:SettingsExpander>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </controls:SettingsGroup>
                    </StackPanel>
                </ContentControl>
            </StackPanel>
        </controls:SettingsPageControl.ModuleContent>
        <controls:SettingsPageControl.PrimaryLinks>
            <controls:PageLink x:Uid="LearnMore_PowerDisplay" Link="https://aka.ms/PowerToysOverview_PowerDisplay" />
        </controls:SettingsPageControl.PrimaryLinks>
    </controls:SettingsPageControl>
</local:NavigablePage>
