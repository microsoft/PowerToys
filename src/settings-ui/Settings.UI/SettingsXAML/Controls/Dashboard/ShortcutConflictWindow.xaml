<Window
    x:Class="Microsoft.PowerToys.Settings.UI.SettingsXAML.Controls.Dashboard.ShortcutConflictWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:converters="using:Microsoft.PowerToys.Settings.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hotkeyConflicts="using:Microsoft.PowerToys.Settings.UI.Library.HotkeyConflicts"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkcontrols="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    Title="PowerToys Shortcut Conflicts"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base" />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Title Bar Area  -->
        <Grid
            Grid.Row="0"
            Padding="8,8,8,8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

            <TextBlock
                VerticalAlignment="Center"
                Text="PowerToys Shortcut Conflicts" />
        </Grid>

        <!--  Main Content Area  -->
        <ScrollViewer
            Grid.Row="1"
            Padding="24"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto"
            ZoomMode="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Description text  -->
                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,0,24"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="Some PowerToys shortcuts conflict with system or other module shortcuts that may cause unexpected behavior. You can modify shortcuts directly or click on a module card to navigate to its settings."
                    TextWrapping="Wrap" />

                <!--  Conflicts List  -->
                <ItemsControl x:Name="ConflictItemsControl" Grid.Row="1" ItemsSource="{x:Bind ViewModel.ConflictItems, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="hotkeyConflicts:HotkeyConflictGroupData">
                            <StackPanel Margin="0,0,0,32">
                                <!--  Conflict Header  -->
                                <StackPanel Margin="0,0,0,12">
                                    <!--  Hotkey Header  -->
                                    <Border
                                        Margin="0,0,0,8"
                                        Padding="12,6"
                                        HorizontalAlignment="Left"
                                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                                        CornerRadius="4">
                                        <TextBlock
                                            FontFamily="Cascadia Code, Consolas, monospace"
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                            Text="{x:Bind Hotkey.ToString()}" />
                                    </Border>

                                    <!--  Instruction text  -->
                                    <TextBlock
                                        FontSize="13"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Style="{StaticResource BodyTextBlockStyle}"
                                        Text="The following modules are using this shortcut:" />
                                </StackPanel>

                                <!--  Module Cards and System Conflict Card  -->
                                <StackPanel>
                                    <!--  PowerToys Module Cards  -->
                                    <ItemsControl ItemsSource="{x:Bind Modules}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="hotkeyConflicts:ModuleHotkeyData">
                                                <tkcontrols:SettingsCard
                                                    Margin="0,0,0,4"
                                                    Click="SettingsCard_Click"
                                                    Description="{x:Bind HotkeyName}"
                                                    Header="{x:Bind ModuleName}"
                                                    HeaderIcon="{ui:BitmapIcon}"
                                                    IsClickEnabled="True"
                                                    Loaded="SettingsCard_Loaded">

                                                    <!--  ShortcutControl with TwoWay binding and enabled for editing  -->
                                                    <controls:ShortcutControl
                                                        x:Name="ShortcutControl"
                                                        MinWidth="140"
                                                        Margin="2"
                                                        VerticalAlignment="Center"
                                                        HotkeySettings="{x:Bind HotkeySettings, Mode=TwoWay}"
                                                        HasConflict="True"
                                                        IsEnabled="True" />

                                                    <!--  Tooltip for the entire card  -->
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip Content="{x:Bind HotkeySettings.ConflictDescription, Mode=OneWay}" />
                                                    </ToolTipService.ToolTip>
                                                </tkcontrols:SettingsCard>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!--  System Conflict Card (only show if it's a system conflict)  -->
                                    <tkcontrols:SettingsCard
                                        Margin="0,0,0,4"
                                        Description="Windows system shortcut"
                                        Header="System"
                                        HeaderIcon="{ui:BitmapIcon Source=/Assets/Settings/Icons/Windows.png}"
                                        IsClickEnabled="False"
                                        Visibility="{x:Bind IsSystemConflict}">

                                        <!--  System shortcut message  -->
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            FontStyle="Italic"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource TextFillColorDisabledBrush}"
                                            Text="This shortcut cannot be changed." />

                                        <!--  Tooltip for system conflict  -->
                                        <ToolTipService.ToolTip>
                                            <ToolTip Content="This shortcut is reserved by Windows and cannot be modified." />
                                        </ToolTipService.ToolTip>
                                    </tkcontrols:SettingsCard>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!--  Empty State (when no conflicts)  -->
                <StackPanel
                    x:Name="EmptyStatePanel"
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="Collapsed">
                    <FontIcon
                        Margin="0,0,0,16"
                        HorizontalAlignment="Center"
                        FontFamily="Segoe Fluent Icons"
                        FontSize="48"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Glyph="&#xE73E;" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="16"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="No conflicts detected" />
                    <TextBlock
                        Margin="0,4,0,0"
                        HorizontalAlignment="Center"
                        FontSize="14"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Text="All shortcuts are working properly" />
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>