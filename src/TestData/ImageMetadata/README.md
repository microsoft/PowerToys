Shared image metadata fixtures for PowerToys tests.

This folder is the canonical source for reusable image metadata test assets such as EXIF, GPS, and XMP samples.

Projects should reference these files using $(RepoRoot) and link or copy them into their test output rather than duplicating the assets per project.
