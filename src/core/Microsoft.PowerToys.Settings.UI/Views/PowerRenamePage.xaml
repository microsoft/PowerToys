<Page
    x:Class="Microsoft.PowerToys.Settings.UI.Views.PowerRenamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:viewModel="using:Microsoft.PowerToys.Settings.UI.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <viewModel:PowerRenameViewModel x:Key="eventViewModel"/>
    </Page.Resources>
    
    <Grid ColumnSpacing="{StaticResource DefaultColumnSpacing}" RowSpacing="{StaticResource DefaultRowSpacing}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutVisualStates">
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideLayoutMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SidePanel.(Grid.Column)" Value="1" />
                        <Setter Target="SidePanel.(Grid.Row)" Value="0" />
                        <Setter Target="SidePanel.Width" Value="{StaticResource SidePanelWidth}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="SmallLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource SmallLayoutMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SidePanel.(Grid.Column)" Value="0" />
                        <Setter Target="SidePanel.(Grid.Row)" Value="1" />
                        <Setter Target="SidePanel.Width" Value="Auto" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical"
                    x:Name="PowerRenameSettingsView">

            <TextBlock x:Uid="PowerRename_Description"
                       TextWrapping="Wrap"/>

            <ToggleSwitch x:Uid="PowerRename_Toggle_Enable"
                          Margin="{StaticResource MediumTopMargin}" 
                          IsOn="{Binding Mode=TwoWay, Path=IsEnabled, Source={StaticResource eventViewModel}}"                          
                           />

            <TextBlock  x:Uid="PowerRename_ShellIntegration"
                        Style="{StaticResource SettingsGroupTitleStyle}"
                        Foreground="{ Binding Mode=TwoWay, Path=TextColor, Source={StaticResource eventViewModel}}"/>

            <ToggleSwitch x:Uid="PowerRename_Toggle_EnableOnContextMenu"
                          Margin="{StaticResource SmallTopMargin}" 
                          IsOn="{Binding Mode=TwoWay, Path=EnabledOnContextMenu, Source={StaticResource eventViewModel}}"
                          IsEnabled="{ Binding Mode=TwoWay, Path=IsEnabled, Source={StaticResource eventViewModel}}"
                           />

            <ToggleSwitch x:Uid="PowerRename_Toggle_EnableOnExtendedContextMenu"
                          Margin="{StaticResource SmallTopMargin}"
                          IsOn="{Binding Mode=TwoWay, Path=EnabledOnContextExtendedMenu, Source={StaticResource eventViewModel}}"
                          IsEnabled="{ Binding Mode=TwoWay, Path=IsEnabled, Source={StaticResource eventViewModel}}"
                           />

            <TextBlock x:Uid="PowerRename_AutoCompleteHeader" 
                       Style="{StaticResource SettingsGroupTitleStyle}"
                       Foreground="{ Binding Mode=TwoWay, Path=TextColor, Source={StaticResource eventViewModel}}"/>


            <ToggleSwitch x:Uid="PowerRename_Toggle_AutoComplete"
                          Margin="{StaticResource SmallTopMargin}" 
                          IsOn="{Binding Mode=TwoWay, Path=MRUEnabled, Source={StaticResource eventViewModel}}"
                          IsEnabled="{ Binding Mode=TwoWay, Path=IsEnabled, Source={StaticResource eventViewModel}}"
                           />

            <TextBlock x:Uid="PowerRename_Toggle_MaxDispListNum"
                       Margin="{StaticResource SmallTopMargin}"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ Binding Mode=TwoWay, Path=TextColor, Source={StaticResource eventViewModel}}"/>
            <muxc:NumberBox SpinButtonPlacementMode="Compact"
                            HorizontalAlignment="Left"
                            Margin="{StaticResource HeaderTextTopMargin}"
                            Value="{Binding Mode=TwoWay, Path=MaxDispListNum, Source={StaticResource eventViewModel}}"
                            Minimum="0"
                            Width="240"
                            Maximum="20"
                            IsEnabled="{ Binding Mode=TwoWay, Path=GlobalAndMruEnabled, Source={StaticResource eventViewModel}}"
                            />

            <ToggleSwitch x:Uid="PowerRename_Toggle_RestoreFlagsOnLaunch"
                          Margin="0, 17, 0, 0" 
                          IsOn="{Binding Mode=TwoWay, Path=RestoreFlagsOnLaunch, Source={StaticResource eventViewModel}}"
                          IsEnabled="{ Binding Mode=TwoWay, Path=IsEnabled, Source={StaticResource eventViewModel}}"
                           />
        </StackPanel>


        <StackPanel x:Name="SidePanel"
                    Orientation="Vertical"
                    HorizontalAlignment="Left"
                    Width="{StaticResource SidePanelWidth}"
                    Grid.Column="1">

            <TextBlock x:Uid="About_PowerRename"
                       Style="{StaticResource SettingsGroupTitleStyle}"
                       Margin="{StaticResource XSmallBottomMargin}"
                       Foreground="{ Binding Mode=TwoWay, Path=TextColor, Source={StaticResource eventViewModel}}"/>

            <HyperlinkButton NavigateUri="https://aka.ms/PowerToysOverview_PowerRename">
                <TextBlock x:Uid="Module_overview" />
            </HyperlinkButton>

            <HyperlinkButton NavigateUri="https://github.com/microsoft/PowerToys/issues">
                <TextBlock x:Uid="Give_Feedback" />
            </HyperlinkButton>


            <TextBlock 
                x:Uid="AttributionTitle"
                Style="{StaticResource SettingsGroupTitleStyle}"
                Foreground="{ Binding Mode=TwoWay, Path=TextColor, Source={StaticResource eventViewModel}}"/>

            <HyperlinkButton 
                NavigateUri="https://github.com/chrdavis/SmartRename">
                <TextBlock Text="Chris Davis's SmartRenamer" TextWrapping="Wrap" />
            </HyperlinkButton>
        </StackPanel>
    </Grid>
</Page>