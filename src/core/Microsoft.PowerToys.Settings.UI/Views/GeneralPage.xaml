<Page
    x:Class="Microsoft.PowerToys.Settings.UI.Views.GeneralPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <StackPanel Orientation="Vertical">
            <muxc:RadioButtons Header="Theme" Margin="{StaticResource SmallTopMargin}">
                <RadioButton Content="Dark"/>
                <RadioButton Content="Light"/>
                <RadioButton Content="System default" IsChecked="True"/>
            </muxc:RadioButtons>

            <ToggleSwitch Header="Start at startup" IsOn="True" Margin="{StaticResource SmallTopMargin}" />

            <ToggleSwitch Header="Disable telemetry" 
                              IsOn="True" 
                              Margin="{StaticResource SmallTopMargin}" />

            <TextBlock Text="PowerToys currently respects the Windows data &amp; feedback setting" 
                           Style="{StaticResource CaptionTextBlockStyle}" />

            <TextBlock Text="Default apps" 
                           Style="{StaticResource SettingsGroupTitleStyle}"/>

            <ComboBox Header="Shell"
                          SelectedIndex="0"
                          MinWidth="240"
                          Margin="{StaticResource SmallTopMargin}">
                <ComboBoxItem>PowerShell</ComboBoxItem>
            </ComboBox>

            <ComboBox Header="Terminal"
                          SelectedIndex="0"
                          MinWidth="240"
                          Margin="{StaticResource SmallTopMargin}">
                <ComboBoxItem>Windows Console</ComboBoxItem>
            </ComboBox>

            <TextBlock Text="About PowerToys"
                           Style="{StaticResource SettingsGroupTitleStyle}"/>

            <TextBlock
                    Text="Version 0.15.1.0"
                    FontWeight="Bold"
                    Margin="{StaticResource SmallTopMargin}" />

            <Button Content="Check for updates"
                        Margin="{StaticResource SmallTopMargin}"
                        Style="{StaticResource AccentButtonStyle}"/>

            <HyperlinkButton Content="Report a bug"
                                 Margin="{StaticResource SmallTopMargin}" />

            <HyperlinkButton Content="Request a feature"  />

            <HyperlinkButton Content="Privacy statement"  />
        </StackPanel>
    </Grid>
</Page>