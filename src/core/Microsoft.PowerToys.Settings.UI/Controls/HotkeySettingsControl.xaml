<UserControl
    x:Class="Microsoft.PowerToys.Settings.UI.Controls.HotkeySettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.PowerToys.Settings.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <StackPanel Orientation="Vertical">
        <ToolTipService.ToolTip>
            <ToolTip>
                <StackPanel Orientation="Vertical">
                    <TextBlock x:Uid="ShortcutWarningLabel"/>
                    <TextBlock Text="Win, Ctrl, Alt, Shift" FontWeight="SemiBold"/>
                </StackPanel>
            </ToolTip>
        </ToolTipService.ToolTip>

        <StackPanel Orientation="Horizontal">
            <local:BodyTextBlock x:Name="TitleText"
                        Margin="0,-12,0,0"
                        Text="{x:Bind Header, Mode=OneTime}"/>
            <local:BodyTextBlock x:Name="TitleGlyph" Text="&#xE946;"
                       FontFamily="Segoe MDL2 Assets"
                       Margin="5,-8,0,0"/>
        
        </StackPanel>
        <TextBox x:Name="HotkeyTextBox"
                 Margin="0,5,0,0"
                 IsReadOnly="True"/>
    </StackPanel>
</UserControl>